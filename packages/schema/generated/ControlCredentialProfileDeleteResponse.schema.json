{
  "$ref": "#/definitions/ControlCredentialProfileDeleteResponse",
  "definitions": {
    "ControlCredentialProfileDeleteResponse": {
      "type": "object",
      "properties": {
        "profile": {
          "type": "object",
          "properties": {
            "profile_id": {
              "type": "string",
              "minLength": 1,
              "description": "Stable user-chosen profile identifier (unique per harness)."
            },
            "harness_id": {
              "type": "string",
              "minLength": 1,
              "description": "Harness family this profile belongs to."
            },
            "display_name": {
              "type": "string",
              "minLength": 1,
              "description": "Human label shown wherever the profile appears."
            },
            "credential_kind": {
              "type": "string",
              "enum": [
                "config_dir_login",
                "oauth_token",
                "api_key"
              ],
              "description": "Binding kind for a profile: Claudexor-owned scoped vendor state, a managed OAuth secret, or a managed API-key secret; a vendor OS-user credential may remain outside that state."
            },
            "isolation_locator": {
              "type": [
                "string",
                "null"
              ],
              "default": null,
              "description": "Canonical absolute path to Claudexor-owned scoped vendor state for config_dir_login bindings; null for secret-ref kinds."
            },
            "secret_ref": {
              "type": [
                "string",
                "null"
              ],
              "default": null,
              "description": "Namespaced secret-store name (e.g. claude_oauth:work) for oauth_token/api_key profiles; null for config_dir_login."
            },
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Disabled profiles are never routable."
            },
            "created_at": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "ISO-8601 timestamp string."
                },
                {
                  "type": "null"
                }
              ],
              "description": "When the profile was registered; null for hand-written config entries.",
              "default": null
            }
          },
          "required": [
            "profile_id",
            "harness_id",
            "display_name",
            "credential_kind"
          ],
          "additionalProperties": false,
          "description": "The removed registry entry."
        },
        "removed": {
          "type": "boolean",
          "const": true
        },
        "credentialCleanup": {
          "type": "string",
          "enum": [
            "config_dir_removed",
            "secret_deleted",
            "none"
          ],
          "description": "What Claudexor-owned state or managed secret was removed with the binding; this does not assert that a vendor OS-user credential changed."
        },
        "cleanupWarning": {
          "type": "string",
          "description": "DEPRECATED (wire-compat only): a unified-model engine never emits it — partial cleanup is a typed retryable error instead of a removed-with-warning receipt."
        },
        "vendorCredentialDisposition": {
          "type": "object",
          "properties": {
            "owner": {
              "type": "string",
              "const": "vendor"
            },
            "state": {
              "type": "string",
              "const": "left_unchanged"
            },
            "scope": {
              "type": "string",
              "const": "os_user"
            }
          },
          "required": [
            "owner",
            "state",
            "scope"
          ],
          "additionalProperties": false,
          "description": "Exact disclosure that profile binding removal left a vendor-owned OS-user credential unchanged; absence preserves legacy receipts."
        }
      },
      "required": [
        "profile",
        "removed",
        "credentialCleanup"
      ],
      "additionalProperties": false,
      "description": "Receipt for a credential-profile removal."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
