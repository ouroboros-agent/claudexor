{
  "$ref": "#/definitions/RunControl",
  "definitions": {
    "RunControl": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "cancel"
          ],
          "description": "Control verb: cancel the run."
        },
        "target": {
          "type": "object",
          "properties": {
            "attemptId": {
              "type": "string",
              "description": "Attempt to target; omitted = the whole run."
            },
            "harnessId": {
              "type": "string",
              "description": "Harness to target."
            },
            "sessionId": {
              "type": "string",
              "description": "Session to target."
            },
            "requestId": {
              "type": "string",
              "description": "Specific request to target."
            }
          },
          "additionalProperties": false,
          "description": "Optional narrowing of what a run control verb targets.",
          "default": {}
        },
        "reason": {
          "type": "string",
          "description": "Human-readable reason for the control."
        },
        "reason_code": {
          "type": "string",
          "enum": [
            "user_cancelled",
            "host_cancelled",
            "owner_task_gone"
          ],
          "description": "Typed cancellation class riding the abort signal (cancel-reason.ts); absent coerces to user_cancelled."
        }
      },
      "required": [
        "kind"
      ],
      "additionalProperties": false,
      "description": "A control verb (cancel) aimed at a run or a narrower target inside it."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
