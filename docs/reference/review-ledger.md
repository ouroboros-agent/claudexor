# Release review findings ledger

Every reviewer finding from a release wave gets exactly one row here with its
adjudicated disposition (CHECKLISTS "Release review protocol (v3)"). The rows
for declined findings feed the next wave's sealed packet as
`DECLINED_FINDINGS.md` — a reviewer re-raising a declined finding without new
evidence is out of scope by construction. Fixed findings cite the batch-fix
commit; deferrals cite their `docs/BACKLOG.md` or `docs/FEATURES.md` row.

Dispositions:

- `fixed` — passed the blocker contract (INV-139); fixed in the wave's one
  batched fix commit (cite it).
- `declined` — rejected with a reason: no invariant/criterion violated,
  re-litigates a recorded owner decision (cite the D#), not reachable in the
  default configuration, or factually wrong.
- `backlog` — real but out of this release's scope; cite the BACKLOG row.
- `features` — accepted limitation; cite the `docs/FEATURES.md` row.

Historical qualified forms keep the same canonical disposition while
preserving why the row was adjudicated: `declined-with-evidence` and
`SUPERSEDED-declined` are `declined`; `duplicate-backlog`, `deferred`,
`recorded`, and `disclosed` are `backlog`; and `accepted-corrected`,
`fixed-with-correction`, `fixed-with-evidence`, `sealed`, and `verified` are
`fixed`. New rows use only the four canonical values above.

| release | wave | reviewer | item | finding (short) | disposition | reference |
| ------- | ---- | -------- | ---- | --------------- | ----------- | --------- |
| 3.0.0 | 1 | sol, critic-engine | B1 | delegation-belt budget: spendUsd had no producer, no reservation at grant (parallel bypass), descriptor from raw not resolved budget | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | sol, critic-surfaces | B2 | runtime updater: install() had zero call sites, no daemon stop/swap/start, first-install strands current.json, tar extraction unsanitized | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | critic-surfaces | B3 | root CHANGELOG.md had no v3.0.0 entry so the publish leg throws | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | immune-scan | B4 | docs-truth sweep: v2-root refs, README active_profile_id, DEVELOPMENT five-pipelines, ARCHITECTURE spec/audit, Bible INV-113/INV-125 wording | fixed | wave-1 batch fix 262070d5; CONCEPT-CHANGE INV-113, INV-125 |
| 3.0.0 | 1 | fable-scope | B5 | ACP question forms (D14): option-less mid-run questions silently skipped; multi collapses to one | fixed | wave-1 batch fix 262070d5; docs/FEATURES.md acp/interactions |
| 3.0.0 | 1 | critic-surfaces | B6 | macOS live bugs: Open Daemon Log v2 path, phase from payload.status not lifecycle, dead ReviewVerdict.ungated + RunMode explore/orchestrate, stale account help | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable-scope | B7 | markRunApplyState writes only delivery_state.yaml but the summary fingerprint missed it, so RunDetail went stale after apply | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | sol | B8 | owner attestation validated structural floors but not the exact triad+scope panel; bind panel slot digests | fixed | wave-1 batch fix 262070d5; CONCEPT-CHANGE INV-125 |
| 3.0.0 | 1 | owner-live | B9 | F1 removal left retired active_profile_id keys in existing v3 config.yaml — strict parse bricked accounts; forward sweep strips known-retired keys with disclosure | fixed | wave-1 batch fix 262070d5 (RETIRED_CONFIG_KEYS, packages/config) |
| 3.0.0 | 1 | sol | A1 | resolveContinuity catch-all swallowed packet-build failures silently | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | critic-surfaces | A2 | CI/release workflows missing pnpm inv:check + fixtures:swift:check | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable | A3 | thread-select needed realpath-normalized repoRoot comparison | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable | A4 | codex native-home override lacked the containment guard claude has | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable-scope | A5 | engineBuildIdentity reported a wrong git sha from an installed copy inside an unrelated repo | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable-scope | A6 | prunableCommandIds pruned needs-decision runs, losing operator visibility parity with old blocked retention | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | fable-scope | A7 | deriveApplyEligibility legacy-vocab arm alignment | fixed | wave-1 batch fix 262070d5 (verified already on the v3 review/checks axes) |
| 3.0.0 | 1 | fable | A8 | composer seeded Workspace write instead of the repo trust access_default | fixed | wave-1 batch fix 262070d5 |
| 3.0.0 | 1 | adjudication | A9 | canary needed a typed operator-decision golden story (replacing old INV-111) | fixed | wave-1 batch fix 262070d5 (packages/canary golden.story.ts INV-116 blockers-visible) |
| 3.0.0 | 1 | fable | D-a | belt caps enforceable-in-sandbox: env vars are not a hard boundary | declined | design: only a delegate=true parent gets a belt; sub-runs get none (parent-side structural guard is the authority; env is defense-in-depth) |
| 3.0.0 | 1 | fable-scope | D-b | GET /threads needs-decision derivation perf | backlog | docs/BACKLOG.md v3.0.0 wave 1 deferrals |
| 3.0.0 | 1 | fable | D-c | council parallel continuity disclosure is last-wins | backlog | docs/BACKLOG.md v3.0.0 wave 1 deferrals |
| 3.0.0 | 1 | fable-scope | D-d | summary pass not integrated with BudgetLedger | backlog | docs/BACKLOG.md v3.0.0 wave 1 deferrals |
| 3.0.0 | 1 | fable-scope | D-e | planRef RUN_START_CLIENT_REJECTED docs nuance | declined | verified accurate: docs/ARCHITECTURE.md Implement/planRef freeze + replay |
| 3.0.0 | 2 | sol, fable | B1 | Bible INV-091 still described the retired two-plane Run Detail/Canvas Workbench (D42); docs across ARCHITECTURE/DESIGN_SYSTEM/README/CHANGELOG named retired tabs, Spec/Audit/orchestrate intents, and `claudexor explore`/`audit` verbs | fixed | wave-2 batch fix; CONCEPT-CHANGE INV-090, INV-091 |
| 3.0.0 | 2 | fable | B2 | interaction-answer regression: D42 deleted TaskDetailView, the only surface rendering InteractionCard, so a mid-run question was unanswerable (AppModel.answerInteraction had no UI caller) | fixed | wave-2 batch fix |
| 3.0.0 | 2 | fable | B3 | updater checkForRuntimeUpdate cached a RuntimeUpdater with the default Noop daemon lifecycle; installRuntimeUpdate reused it, so every real install no-op'd the stop/swap and rolled back | fixed | wave-2 batch fix |
| 3.0.0 | 2 | sol, fable | B4 | stopIdleDaemon SIGTERM'd a raw writer-lease pid with no kernel-start-identity proof (recycled-pid risk), install ignored its false return before the swap, and rollback relaunched without death-proving the failed new runtime | fixed | wave-2 batch fix |
| 3.0.0 | 2 | sol, fable | B5 | SECURITY: VendorIdentityLoader read ordinary vendor credential stores (~/.codex/~/.claude token-bearing files, INV-067) and misattributed a native-login row's identity; app-side read removed (INV-002). SHIPPED IN 3.0 the boundary-respecting replacement — a daemon-side NON-SECRET {email, plan} projection read ONLY from each account's OWN Claudexor-owned store (never a vendor file, never the app), allowlisted so no token material crosses the wire | fixed | wave-2 batch fix (removal); daemon-side identity: packages/harness-codex/src/profile.ts codexAccountIdentity + packages/harness-claude/src/native-home.ts claudeAccountIdentity, AccountIdentity on ControlHarnessAccounts + credential-profile entry, rendered by AccountsPresentation.identityLine |
| 3.0.0 | 2 | sol | B6 | apply-after-accept-risk hidden: applyLoadKey omitted riskAccepted so the eligibility reload never fired, and the workspace suppresses decision-flow apply, leaving an accepted-risk run with no apply path | fixed | wave-2 batch fix |
| 3.0.0 | 2 | sol | B7 | release.yml review_attestation_b64 input still said schema-v2 while validateReleaseAttestation rejects any non-v3 attestation | fixed | wave-2 batch fix (scripts/release-workflow-check.mjs assertion) |
| 3.0.0 | 2 | fable, sol | A1 | orchestrator→@claudexor/mcp-server layering inversion (belt fix imported DELEGATION_ENV upward) | fixed | wave-2 batch fix (moved to @claudexor/util; dep dropped) |
| 3.0.0 | 2 | fable | A2 | sweepRetiredConfigKeys rewrote config.yaml without withConfigLock (race with the daemon's config writers) | fixed | wave-2 batch fix |
| 3.0.0 | 2 | fable | A3 | isApiKeyMetaHost gated only .raw while comments/help imply openrouter too; the openrouter raw-API instance is a real api-key meta-host | fixed | wave-2 batch fix |
| 3.0.0 | 2 | fable | A4 | applyStateRow ("Applied · review blocked") dropped from TurnCard in D42 — a real delivery state lost its only chat-inline render (INV-093) | fixed | wave-2 batch fix (restored via RunFacts.applyFact) |
| 3.0.0 | 2 | sol | A5 | ratchet baselines carried unjustified slack from D42/batch6 (Models 688→542, DomainModels 750→658, ThreadsScreen 598→591, cli.ts 1475→1456); AppModel/acp verified tight | fixed | wave-2 batch fix (--update re-tighten) |
| 3.0.0 | 2 | sol | A6 | ArtifactGalleryView serialized N produced-endpoint round-trips for a thread-aggregated gallery | fixed | wave-2 batch fix (concurrent fan-out over the Sendable client) |
| 3.0.0 | 2 | sol | D1 | 9314ecfd never got a full-candidate review wave | declined | reasoned cumulative-review protocol: wave-1 (b275d576..fcd0b69d) ∪ wave-2 (fcd0b69d..9314ecfd) is the full tag candidate; the post-batch confirmation wave re-covers the fix delta |
| 3.0.0 | 2 | fable | D2 | packet hygiene: stale DECLINED_FINDINGS template + PHASE_DELTA self-note in the confirmation packet | declined | process, not a product blocker: fix the packet BUILDER to pull the real ledger for the confirmation packet |
| 3.0.0 | confirm | sol | C1 | runtime in-app auto-INSTALL flow (AppModel install action → RuntimeUpdater.install): flagged across three waves (B2/B3/B4) and re-raised — a missing idle-check could kill an active run | features | deferred to 3.1 per owner-locked D1 (M7 split-to-3.1, pre-approved); shipped CHECK-only; docs/FEATURES.md macos/updater. Install flow + its tests deleted in the confirmation fix |
| 3.0.0 | confirm | sol | C2 | DefaultRuntimeDaemonLifecycle stop/swap/relaunch reimplementation (RuntimeProbe.swift): PID-only TERM + rollback mutating current.json on unconfirmed daemon death is security-sensitive process-killing code | features | deferred to 3.1 per D1; RuntimeProbe.swift (probe/handshake/lifecycle/process-identity) + DaemonLifecycleStopTests deleted; docs/FEATURES.md macos/updater |
| 3.0.0 | confirm | sol | C3 | RuntimeInstaller unpack/swap/rollback + RuntimeUpdaterTests lifecycle-order/PID-reuse cases — the install WRITE side, unreachable once install is deferred | features | deferred to 3.1 per D1; RuntimeInstaller trimmed to the current.json READ (DaemonLauncher pointer resolution + running-engine version); install tests deleted, check + resolution tests kept; docs/FEATURES.md macos/updater |
| 3.0.0 | confirm | sol | C4 | docs still taught the deleted Active/Use account concept: README "Credential Profiles And Quota" (Active marker/precedence/clear-on-delete) + DESIGN_SYSTEM §4 (Use/Using, "Use atomically makes that harness primary") + README screenshot alt text "Use" | fixed | confirmation fix — rewritten to the INV-135 live model (Enabled toggle in the pool + informational server-computed next-up + composer per-thread pin) |
| 3.0.0 | confirm | sol | A1 | RunArtifactsAccess listing-accessor naming (runArtifacts / producedArtifacts) | declined | verified: the run-tree vs produced-outputs accessors are consistently named and each carries a doc comment; no functional issue |
| 3.0.0 | confirm | sol | A2 | a README "v3.0" status sentence points at the "Stability at 2.0" section | declined | verified: the anchor resolves (#stability-at-20) and the section deliberately scopes "the clean v2 contract" — the semver stability promise was frozen at 2.0 and is unchanged at 3.0, an intentional historical anchor, not a stale path |
| 3.0.0 | confirm | sol | A3 | DomainModels openrouter comment (registry.ts createRawApiAdapter id "openrouter") | declined | verified accurate: packages/cli/src/registry.ts calls createRawApiAdapter({ id: "openrouter", … }); the comment matches source |
| 3.0.0 | confirm | sol | A4 | B6: is the .task(id: applyLoadKey) accept-risk→apply reload inert? | declined | verified working: the reload fires from the DecisionBar accept-risk closure (await model.loadRunDetail); TurnCard.run is computed from model.task(id), so the refreshed eligibility re-renders Apply — not inert |
| 3.0.0 | confirm | sol | A5 | rollback prior-runtime edge (install rollback restores current.json on unconfirmed daemon death) | declined | moot: the entire install/rollback path is deferred to 3.1 (C1-C3 / D1), so the edge cannot occur in 3.0 |
| 3.0.0 | confirm | sol | A6 | ARCHITECTURE cumulative-review paragraph wording | declined | verified accurate: the INV-125 owner-review protocol (≥2 independent reviewers, ≤3 rounds, any mutation invalidates + re-freezes, schemaVersion-3 attestation binding SHA/tree/gate-receipt/reviewer digests) is described correctly |
| 3.0.0 | final | sol | doc-routing | README/DESIGN_SYSTEM overclaimed routing auto-picks next_up among all enabled accounts | fixed | commit dca62e41 (final doc) (unpinned→CLI-login default; named accounts route only via pin/quota-rotation; next_up informational) |
| 3.0.0 | final | sol | doc-pin | README claimed a thread auto-remembers its first account | fixed | commit dca62e41 (final doc) (thread pins only an explicit composer choice) |
| 3.0.0 | final | sol/fable | doc-manifest | CHANGELOG said the update reads a "signed" release manifest | fixed | signature field is reserved; reworded |
| 3.0.0 | final | fable | swift-comments | AccountsPopover comments still said "active" account | fixed | reworded to "in-effect" |
| 3.0.0 | final | fable | cli-check-story | CLI release-command not updated for check-only | declined | verified: CLI already says check + `npm install -g claudexor@latest`, no auto-install claim |
| 3.0.0 | final | sol | chip-stale-avail | checkForRuntimeUpdate catch keeps prior .available | declined | a transient check failure should not erase a real prior availability; the status line discloses the failure |
| 3.0.0 | final | sol | engine-version-src | resolvedRunningEngineVersion trusts readCurrent().version vs DaemonLauncher fallback | backlog | cosmetic version display; docs/BACKLOG.md |
| 3.0.0 | final | fable | packet-hygiene | packet TESTS.txt carried stale counts | fixed | sealed-packet evidence file (packet-src TESTS.txt), not a repo artifact — made count-agnostic |
| 3.0.0 | final-2 | sol | rounds-cap-relitigation | argued the 3→10 rounds-cap raise weakens the attestation floor | declined | re-litigates the recorded owner decision (owner: "повысь кап до 10 раундов"; rationale in ef996872 + ARCHITECTURE) — out of scope by the blocker contract |
| 3.0.0 | final-2 | fable | dev-doc-rounds | docs/DEVELOPMENT.md still stated the <=3 rounds structural floor | fixed | final docs commit (DEVELOPMENT.md synced to OWNER_REVIEW_MAX_ROUNDS=10) |
| 3.0.2 | 1 | sol, fable, critic-code, critic-docs | C1 | CHANGELOG v3.0.2 entry spliced OVER the shipped v3.0.1 header — v3.0.1 lost its entry, DMG-hotfix prose misattributed to 3.0.2, and the publish leg would bake the mangled blob into immutable release notes | fixed | wave-1 batch fix (v3.0.1 bullet restored; 3.0.2 entry ends at "author section.") |
| 3.0.2 | 1 | critic-code | C2 | darwin keychain-only gate (owner Q2=a) had no direct test: a present .credentials.json plus platform "darwin" was never exercised, so a regression flipping the gate would pass the suite | fixed | wave-1 batch fix (darwin-gate test, deterministic on macOS and Linux) |
| 3.0.2 | 1 | critic-docs | C3 | review ledger had zero rows for the 3.0.1 and 3.0.2 waves while the protocol requires every finding ledgered | fixed | these 3.0.2 rows land in the wave-1 batch fix; the 3.0.1 gap is disclosed to the owner (its round evidence is retained outside the repository, in gitignored operator records) — backfilling adjudications this executor did not witness would fabricate dispositions |
| 3.0.2 | 1 | sol, critic-code | A1 | oauth/usage 200 response whose body parses to null yields neither snapshot nor typed absence (pre-existing, byte-identical at base; needs a nonstandard/changed vendor response) | backlog | docs/BACKLOG.md "v3.0.2 review wave deferrals" Q-a |
| 3.0.2 | 1 | fable | A2 | claude/codex quota sources live in packages/cli (surface package) rather than a control-plane package — pre-existing structural placement, tests already sit at the owning boundary | backlog | docs/BACKLOG.md Q-b |
| 3.0.2 | 1 | fable, critic-code | A3 | docs say "on Linux" while the code arm is all non-darwin ("off macOS") | declined | shorthand within the README's stated support envelope (macOS app; CLI/daemon on Linux); CHANGELOG already says "Off macOS"; no supported platform is misdescribed |
| 3.0.2 | 1 | critic-code | A4 | file read has no size cap (keychain leg has maxBuffer 1 MiB) | declined | unreachable in default config: the vendor writes the 0600 file inside the Claudexor-owned config root; a multi-GB credentials file is not a real state of the supported system — fold into Q-a's hardening if that row ships |
| 3.0.2 | 1 | critic-docs, fable-scope | A5 | should docs/FEATURES.md carry a "linux quota live-smoke pending" row? | declined | scope slot verdict: the hermetic ubuntu-CI tests ARE the executable check (Zen #6); the live-smoke deferral is disclosed in RELEASE_PREFLIGHT.md and the wave report, and the README claims nothing beyond the tested behavior |
| 3.0.2 | 1 | fable (r7 residue) | A6 | review devtool: retry eligibility decided before full checklist validation + retried slot discards first-attempt telemetry (two 3.0.1-r7 sol criticals, re-observed live this wave when the gemini slot failed its liveness floor with no retry) | backlog | docs/BACKLOG.md Q-c; devtool-only, unreachable in shipped product |
| 3.0.2 | confirm | sol | C4 | wave-1 ledger row C3 embedded a machine-local operator path in tracked public documentation (docs-hygiene: no local paths) | fixed | confirmation batch fix (row reworded to "retained outside the repository, in gitignored operator records") |
| 3.0.2 | confirm | sol | A7 | credential-file read follows symlinks and does not require a regular file (locally planted symlink could point the reader outside the owned store; not reachable untampered) | backlog | docs/BACKLOG.md Q-a extended: no-follow open, fstat regular-file check, bounded read, rejection tests |
| 3.0.2 | confirm | fable | A8 | does the accounts identity line (email · plan) share the quota reader's old macOS-only gap on Linux? | declined | verified in code: claudeAccountIdentity (packages/harness-claude/src/native-home.ts) reads the NON-secret .claude.json directly from the owned config dir via readFileSync — platform-neutral, no keychain involvement; the owner's post-release Linux smoke will observe it live as a bonus |
| 3.0.3 | 1 | triad-fable | C1 | Swift SetupNativeCommandErrorCode lacked device_auth_unsupported — strict decode fails in the exact old-CLI scenario | fixed | wave-1 batch abf4c11c (SetupEvidenceModels + decode/invariant tests) |
| 3.0.3 | 1 | triad-fable, security-critic | C2 | tee'd outputTail persisted without redactSecrets (INV-062 defense-in-depth) | fixed | abf4c11c (boundedTail redactSecrets + no-ESC/redaction test) |
| 3.0.3 | 1 | docs-critic, contracts-critic | C3 | R6 device-auth-disabled remedy not delivered; packet claimed it was | fixed | abf4c11c (flow-keyed toggle remedy on command_failed; DECIDED corrected) |
| 3.0.3 | 1 | triad-fable | A1 | ANSI-strip claimed missing ESC prefix | declined | factually wrong (byte-verified real ESC in source); superseded by the C2/OSC upgrade |
| 3.0.3 | 1 | security-critic, contracts-critic | A2 | strip covered CSI only; OSC/bare-ESC/C0 survive into journaled messages | fixed | abf4c11c (full CSI+OSC+C0 scrub, newline kept) |
| 3.0.3 | 1 | correctness-critic, triad-fable | A3 | probe/tee settled on exit (truncation race) and probe lacked exit-code gate (chatty old CLI misclassified) | fixed | abf4c11c (close-settle; exit-0 + nonempty = completed; chatty fail-open test) |
| 3.0.3 | 1 | correctness-critic | A4 | reconcile TOCTOU: a login finishing inside the restart window discarded as cancelled_on_restart | fixed | abf4c11c (late-result re-check in both branches) + wave-2 probe-hook test |
| 3.0.3 | 1 | correctness-critic | A5 | cancel racing daemon death leaves an unfinishable cancelling zombie | fixed | abf4c11c (successor completes the interrupted termination) |
| 3.0.3 | 1 | correctness-critic | A6 | post-login quota blindness up to 15min (absence backoff not reset) | fixed | abf4c11c (noteCredentialChange on the credential hook + test) |
| 3.0.3 | 1 | triad-fable | A7 | create() dedupe ignored loginFlow — redirect request silently answered with the active device-auth job | fixed | abf4c11c (typed 409 on flow mismatch + test) |
| 3.0.3 | 1 | triad-fable, contracts-critic | A8 | quota-registry backoff + oauth 200-null tests promised by the plan did not land; records over-claimed | fixed | abf4c11c (daemon-boundary backoff/reset tests; oauth null-body test); packet corrected |
| 3.0.3 | 1 | triad-fable | A9 | ARCHITECTURE self-contradiction: "vendor output is not copied into durable logs" vs the tail | fixed | abf4c11c (sentence qualified) |
| 3.0.3 | 1 | triad-fable | A10 | HarnessDefaultsRow stale budget-cap comment | fixed | abf4c11c |
| 3.0.3 | 1 | triad-fable | A11 | interrupted outcome value now producer-less | fixed | abf4c11c (documented historical, kept for journal replay) |
| 3.0.3 | 1 | triad-fable | A12 | shell-exported CLAUDEXOR_CONFIG_DIR + default-root artifact refuses with an unfixable remedy | fixed | abf4c11c (refusal message names the env-inherited case) |
| 3.0.3 | 1 | triad-fable | A13 | AGENT_ONBOARDING carried retired pre-v3 verbs (orchestrate/audit/explore) in the promoted canonical guide | fixed | abf4c11c |
| 3.0.3 | 1 | docs-critic | A14 | S13(b) CLI help pointer silently undelivered | fixed | abf4c11c (help epilogue) |
| 3.0.3 | 1 | docs-critic | A15 | BACKLOG Q-a row stale vs the shipped half | fixed | abf4c11c (row split: shipped half marked, hardening stays open) |
| 3.0.3 | 1 | security-critic | A16 | handshake echoed the daemon-reported version unguarded | fixed | abf4c11c (version-shape guard) |
| 3.0.3 | 1 | security-critic | A17 | "any browser context / cannot disturb" over-claim in code comment + generated schema describe | fixed | abf4c11c (hedged; schema regenerated) |
| 3.0.3 | 1 | contracts-critic | A18 | config_invalid advertised .bak-* for trust/env paths that never have backups | fixed | abf4c11c (conditional on config.yaml paths + test) |
| 3.0.3 | 1 | contracts-critic | A19 | project-sweep backup byte-identity untested | fixed | abf4c11c |
| 3.0.3 | 1 | contracts-critic | A20 | device_auth_unsupported remedy named the API field, not the CLI flag | fixed | abf4c11c |
| 3.0.3 | 1 | correctness-critic | D5 | updateGlobalConfig strips retired keys without a backup on any root | backlog | docs/BACKLOG.md (settings-write backup row) |
| 3.0.3 | 1 | contracts-critic | D6 | retired-key gate blind to imported sub-schemas; extract-refactor trap | backlog | docs/BACKLOG.md (gate hardening row) |
| 3.0.3 | 1 | contracts-critic | D7 | 422 test uses a duck-typed literal, not the real ConfigParseError class | declined | two-sided pinning accepted; integration case noted |
| 3.0.3 | 2 | triad-sol | W1 | protected gate/test edits lack typed protectedPathApprovals receipts | declined | approvals are the PRODUCT-run surface; authored owner-program edits are covered by AUTHORIZATION.json + this review (3.0.x precedent) |
| 3.0.3 | 2 | triad-sol | W2 | device one-time code can persist in the failed-login tail | declined | re-litigates the wave-1 adjudicated tradeoff (code is not a bearer secret, written post-exit, 0600 + token-authed local surfaces, redactSecrets applied) |
| 3.0.3 | 2 | triad-sol | W3 | probe output retained unbounded for up to 10s | fixed | wave-2 close commit (64 KiB retention cap) |
| 3.0.3 | 2 | triad-sol | W4 | shared mutable lastRefreshSnapshotCount races concurrent refreshes | fixed | wave-2 close commit (cycle-local refreshCycle) |
| 3.0.3 | 2 | triad-fable | W5 | restart-completed cancel misattributes a timeout as cancelled_by_user | fixed | wave-2 close commit (durable-deadline reason replay) |
| 3.0.3 | 2 | triad-fable | W6 | mojibake em-dash in the boundedTail comment | fixed | wave-2 close commit |
| 3.0.3 | 2 | ledger-critic | W7 | wave-1 bare `claude auth login` doctor remedy had no disposition (INV-067) | fixed | wave-2 close commit (all six doctor remedies now name `claudexor auth login <harness>`) |
| 3.0.3 | 2 | ledger-critic, delta-critic | W8 | zero v3.0.3 rows in this ledger (S14 undelivered) | fixed | this section |
| 3.0.3 | 2 | ledger-critic | W9 | packet record errors: test counts (10/111 vs 9/96+Swift-extended), two TRACEABILITY misattributions, DECIDED precision | fixed | wave-2 reseal (packet-only) |
| 3.0.3 | 2 | delta-critic | W10 | close-wait can hang the codex tee leg on a pipe-holding grandchild | backlog | docs/BACKLOG.md (bounded by the 15-min job deadline; vendor-contingent) |
| 3.0.3 | 2 | delta-critic | W11 | prefix-anchored redaction can miss a secret straddling the tail slice boundary | backlog | docs/BACKLOG.md (defense-in-depth edge) |
| 3.0.3 | 2 | delta-critic | W12 | unterminated-OSC over-strip erases trailing diagnostics | declined | safe-direction cosmetic loss; terminal-faithful |
| 3.0.3 | 2 | delta-critic | W13 | unguarded quotaStoreSlot.current() in the credential hook | declined | practically shielded (slots bind/clear as a unit); convention nit |
| 3.0.3 | 2 | delta-critic | W14 | CHANGELOG "always carries the remedy" wider than the command_failed path | fixed | wave-2 close commit (wording) |
| 3.0.3 | 2 | panel-infra | W15 | round-2 scope slot died: prompt 1.008M tokens > 1M provider cap | fixed | round-3 rerun with a reduced evidence pack (transport bound, not a panel substitution) |
| 3.0.3 | 1-2 | immune-scan | T1 | INV-137 note calls a pnpm-test suite a "canary" | backlog | docs/BACKLOG.md (wording) |
| 3.0.3 | 1-2 | immune-scan | T2 | one ratchet baseline sits above the shrunk mcp-server file | declined | tightening is --update hygiene, not release work |
| 3.0.3 | 3 | triad-sol | W16 | harness-cursor auto/no-key doctor reason still named bare `cursor-agent login` (missed by the W7 grep pattern) | fixed | wave-3 close commit (+ source-level INV-067 remedy test) |
| 3.0.3 | 3 | triad-sol | W17 | probe retention cap counted UTF-16 units, not bytes; split-UTF-8 decode risk | fixed | wave-3 close commit (Buffer accumulator, decode-once) |
| 3.0.3 | 3 | triad-sol | W18 | README credential-profiles row still says "log in/out through the vendor CLI" | fixed | wave-3 close commit (wording aligned with INV-067) |
| 3.0.3 | 3 | triad-fable | W19 | reason replay evaluates deadlineAt at successor time — a daemon-down window past the deadline replays a user cancel as timed_out | declined | both are honest cancel-family terminals; persisting the in-flight reason needs a schema field (disproportionate; noted for a future sidecar rev) |
| 3.0.3 | 3 | triad-fable | W20 | `claudexor profiles login` bypasses the daemon so noteCredentialChange never fires — up to 15min quota absence latency | backlog | docs/BACKLOG.md W-f (credential-changed nudge on the control API) |
| 3.0.3 | 3 | triad-fable | W21 | 3.0.3 runner result read by a pre-3.0.3 daemon rejects the unknown outputTail key (upgrade-mid-login window only) | declined | unreachable in the bundled single-version default; upgrade guidance noted |
| 3.0.3 | 3 | triad-fable | W22 | DECLINED D1-D4 rows absent from the durable ledger (numbering starts at D5) | fixed | wave-3 close commit (rows added below) |
| 3.0.3 | pre | audit | D1 | profiles-login lacks flow opt-out/probe/isolation note | declined | deferred (plan S6 partial-thread decision); BACKLOG candidate next cycle |
| 3.0.3 | pre | audit | D2 | codex-only login conditionals not declared capabilities at n=1 | declined | data-drive when a second flow-bearing harness exists |
| 3.0.3 | pre | audit | D3 | quota test writes auth.json under the hermetic default home without a guard assert | declined | hermetic via vitest global override; guard assert is test hygiene |
| 3.0.3 | pre | audit | D4 | RetiredKeySweep.backupPath absent from the daemon log line | declined | disclosure exists in the sweep warn; duplicate logging declined |
| 3.0.3 | 3 | triad-sol | W23 | createTailBuffer (the tee tail, twin of the probe cap) kept a JS string and .slice(-4096) — UTF-16 miscount + per-chunk decode split multibyte UTF-8 | fixed | wave-3 close (byte-accurate Buffer ring, decode-once; split-UTF8 test) |
| 3.0.3 | 3 | triad-sol | W24 | ARCHITECTURE/DEVELOPMENT still describe ">=2 reviewers / <=10 rounds" alongside the exact-triad protocol | declined | accurate: the WORKFLOW enforces the structural floors (>=2, <=OWNER_REVIEW_MAX_ROUNDS) while CHECKLISTS holds the exact panel as process law — the docs already say so; a doc-dedup pass is BACKLOG, not release work |
| 3.0.3 | 3-6 | panel-infra | W25 | anthropic (fable triad + scope) slots died on the OpenRouter->Vertex 1M-token prompt cap at full/mid packs; embedded delta-files bloated the packet | fixed | delta-files moved into the packet (full changed-file text, transport-independent) + reduced evidence pack so every required slot fits under 1M |
| 3.0.3 | 3 | owner | W26 | anthropic OpenRouter slots capped at 1M tokens (fable triad slot live only in round 4; scope slot never) | fixed | owner decision 2026-07-21: the scope slot ran on NATIVE claude-fable-5 (same panel model, file-reading transport instead of the capped OpenRouter route); Opus and gpt-5.6-sol ran as ADDITIONAL reviewers and are sealed as extra reports — the attestation panel stays the exact triad+scope contract |
| 3.0.3 | 3 | scope-sol | W27 | setup-job reducer forbade waiting_for_input/running -> not_supported: the device_auth_unsupported path threw invalid_setup_transition and journal replay repeated it at daemon start | fixed | wave-3 final commit (transition registry + end-to-end old-CLI test) |
| 3.0.3 | 3 | scope-sol | W28 | ARCHITECTURE/WHITEPAPER claimed explicit cancel is the ONLY login terminator, contradicting the 15-min deadline | fixed | wave-3 final commit (both claims name cancel + deadline) |
| 3.0.3 | 3 | scope-sol | W29 | INTEGRATIONS login section omitted the S13-promised login-survival semantics | fixed | c46ed49d (the wave-3 edit missed its anchor; landed as a docs-only follow-up, caught by both delta confirmations) |
| 3.0.3 | 3 | scope-sol | W30 | tee ring can start mid-codepoint and decode a leading replacement char | fixed | wave-3 final commit (leading continuation-byte trim) |
| 3.0.4 | 1 | triad-sol + critic-B | X1 | sealed-packet narrative files (DIFF_SUMMARY/PHASE_DELTA/RUNTIME) named the pre-amend candidate dba9c1e8 while FREEZE/FINGERPRINTS/COMMITS/TESTS bound 2e42395e — two SHAs in one packet (the v3.0.3 failure class) | fixed | packet regenerated from FREEZE.json for the confirmation wave; narrative headers now derive from FREEZE |
| 3.0.4 | 1 | triad-sol | X2 | test/gate file edits (AppModelRefreshTests, WireFixtureRoundTripTests) carried no typed protectedPathApprovals evidence | declined | authored directly in the owner session (Claude Code), not through the claudexor typed run surface — protectedPathApprovals applies to claudexor-driven runs; owner authorship recorded in AUTHORIZATION.json |
| 3.0.4 | 1 | triad-sol + triad-fable + critic-B | X3 | D1 (no follow-up GET after save) had no test fence: the stub's GET case would satisfy a re-introduced refresh, and the applied snapshot was never asserted | fixed | adjudication commit: GET /v2/settings call counter asserted 0 on the save path; POST answer asserted APPLIED (interactionTimeoutMs + routing fields) |
| 3.0.4 | 1 | critic-A | X4 | setNativeCredentialsEnabled/setHarnessEnabled kept a redundant await refreshSettings() after saveSettings — a transiently failing GET after a successful save could overwrite "Saved engine defaults." with a load error (the #20 symptom class on a narrow path) | fixed | adjudication commit: both call-site refreshes dropped; the save answer is the snapshot (D1), refreshCredentialProfiles retained |
| 3.0.4 | 1 | critic-A | X5 | three comments still named the dropped post-save refreshSettings() as the republish mechanism (HarnessDefaultsRow x2, AppModel+CredentialProfiles) | fixed | adjudication commit: reworded to "post-save snapshot republish" (line-count neutral, ratchet respected) |
| 3.0.4 | 1 | critic-B | X6 | TRACEABILITY row overstated D1 coverage (cited a model.settingsSnapshot assertion that did not exist) | fixed | packet TRACEABILITY corrected; the assertion now EXISTS (X3), making the row true |
| 3.0.4 | 1 | critic-B | X7 | CHANGELOG "no follow-up GET" claim was true only for the panel save path; accounts toggles still double-fetched | fixed | X4 makes the claim globally true; no CHANGELOG wording change needed |
| 3.0.4 | 1 | immune-scan | X8 | ~23 further GatewayClient response DTOs have no wire-fixture pin — the #20 defect class stays reachable on those endpoints (representative-not-exhaustive doctrine allows it) | backlog | docs/BACKLOG.md W-i (grow response-side wire-fixture coverage endpoint by endpoint) |
| 3.0.4 | 2 | triad-sol | X10 | concurrent row saves: a LATE response from an older POST /settings could overwrite the newer save's applied snapshot (stale UI truth until any refresh) — reachable by editing two harness rows quickly | fixed | targeted-fix commit: saveSettings generation guard (only the newest issued save applies its answer), deterministic response-reorder regression test (stub handlers moved off the serial loading queue), AppModel settings split to AppModel+Settings.swift for the ratchet |
| 3.0.4 | 2 | confirm-critic | X9 | AccountsPopover enabledAction comment ("Both reload the projection after") no longer describes the setHarnessEnabled leg after X4 | declined | unchanged code, note-grade; next docs tidy sweep |
| 3.0.4 | 2 | triad-sol + triad-fable | X11 | packet DIFF_SUMMARY commit list mislabeled the #20 fix commit with the confirmation-batch SHA (regen sed rewrote entry 1) | fixed | commit list now machine-derived from git log base..candidate at packet build |
| 3.0.4 | 2 | triad-fable | X12 | BLOCKER_FILTER still said "This is wave 1: the full diff is reviewed exactly once here" inside the confirmation packet — wave-role contradiction with PHASE_DELTA | fixed | wave-role line regenerated with the packet per wave |
| 3.0.4 | 2 | triad-sol | X13 | hash-bound full-gate receipt for the final candidate pending at confirmation time | declined | receipt is BY DESIGN generated after the review closes: run-full-gate-receipt.mjs runs on the exact final SHA immediately before sealing; the attestation refuses to seal without it (truthful pre-seal state: pending until that step) |
| 3.0.4 | 3 | triad-sol | X14 | X10 generation guard keyed on client ISSUE order, not daemon COMMIT order: an older-issued save committing last could be dropped while carrying the freshest truth, and settingsStatus/refreshHarnesses ran unguarded on superseded saves | fixed | round-3 fix: ALL settings ops serialized through one chain (single in-flight POST, answers apply in queue order == commit order under the daemon config lock); tests reworked to prove B's POST never enters the stub while A is in flight + newer-save-fails case; pin-proven (chain removed -> saveBArrived==1 and 111000 wins, observed) |
| 3.0.4 | 3 | triad-fable | X15 | refreshSettings GET raced saveSettings with no ordering fence (pre-save GET resolving late could overwrite the applied save answer) | fixed | refreshSettings rides the same serialized chain |
| 3.0.4 | 3 | triad-fable | X16 | client-side ordering cannot see DAEMON-side request reordering across connections; full closure needs a server-side monotonic revision/etag on ControlSettingsSnapshot | backlog | docs/BACKLOG.md W-j; client serialization (X14) removes the app-side reordering entirely — one POST in flight |
| 3.0.4 | 3 | triad-sol | X17 | race test used an unbounded while-yield wait (regression would hang swift test instead of failing) | fixed | bounded ~5s deadline with #require |
| 3.0.4 | 3 | triad-fable | X18 | RELEASE_PREFLIGHT.md slipped the FREEZE-derived regen and named the prior candidate (X1 residual class) | fixed | preflight header stamped from FREEZE.json at packet build |
| 3.0.4 | 3 | triad-fable | X19 | test-stub static handler is nonisolated(unsafe), reassigned mid-test with thread-per-request dispatch — a TSan-visible test-infra race | declined | test-only surface, suites .serialized, no gate runs TSan; noted for a test-hygiene sweep |
| 3.0.4 | 4 | triad-fable | X20 | enterHardOffline never retired the queued settings chain: an op enqueued pre-offline could fire against a future reconnected client with a stale patch | fixed | round-4 fix: settingsEpoch bumped in enterHardOffline; every chained op captures its epoch at enqueue and retires inert on mismatch |
| 3.0.4 | 4 | triad-fable | X21 | BLOCKER_FILTER wave-role section froze at the round-3 text (regression of the X12 class claimed fixed) | fixed | wave-role section now MACHINE-STAMPED from FREEZE.json + round inputs at every packet regen, alongside the X18 preflight stamp |
| 3.0.4 | 4 | triad-sol | X22 | ledger X13 claimed the final-SHA receipt already executed (past tense) while preflight said pending | fixed | X13 reworded to a truthful declined disposition (attestation-time prerequisite enforced by the verifier) |
| 3.0.4 | 4 | triad-fable | X23 | round-3 gemini implausible slot had no recorded typed disposition | fixed | recorded: FAILED-implausible, no same-SHA retry (candidate superseded by the accepted X14 fix); the attestation binds the FINAL round's full live panel |
| 3.0.4 | 5 | triad-sol + triad-fable | X24 | epoch guard checked only BEFORE the await: a response landing past an enterHardOffline reset repopulated the cleared projection/status | fixed | round-5 fix: post-await epoch re-check in both chained ops; pin-proven (guard removed -> late answer repopulates snapshot+status, observed) |
| 3.0.4 | 5 | triad-fable | X25 | X20 shipped without a focused test fence | fixed | hardOfflineRetiresInFlightAndQueuedSettingsSaves: in-flight save + offline -> late answer not applied, no status over the reset, queued save never reaches the wire |
| 3.0.4 | 5 | triad-fable | X26 | packet DECLINED_FINDINGS was stale (confirmation-era, missing X9/X19/X13) | fixed | regenerated from the ledger's declined rows at packet build |
| 3.0.4 | 5 | triad-sol | X27 | RELEASE_PREFLIGHT narrative still described the wave-1-era state | fixed | preflight narrative rewritten from FREEZE + the full round history at packet build |
| 3.0.4 | 5 | triad-sol | X28 | X22 merged two findings in one ledger row (one-row-per-finding rule) | fixed | split into X22 (tense) + X23 (gemini disposition) |
| 3.0.4 | 5 | panel-infra | X29 | round-5 gemini slot implausible (liveness floor) | fixed | typed disposition: FAILED-implausible, no same-SHA retry (candidate superseded by the accepted X24 fix); the final round-6 seal requires the full live panel |
| 3.0.4 | 6 | triad-sol | X30 | enterHardOffline dropped the chain tail: after reconnect a new-epoch settings op could run concurrently with the still-in-flight old request, breaking the X14 single-chain criterion (INV-002) | fixed | cycle-6 fix: epoch bump only, chain tail SURVIVES offline (old ops retire inert through their epoch guards); deterministic reconnect fence reconnectSaveWaitsForTheOldInFlightRequest (pin-proven: tail-drop restored -> new save reaches the wire mid-flight, observed); internal adoptClientForReconnect test seam |
| 3.0.4 | 6 | owner-session | X31 | fix-cycle 6 exceeds the AUTHORIZATION reviewRounds.max=5 set at wave 1 | declined | owner-session decision, recorded openly: X30 is a proven regression introduced BY the review cycle itself (X20's tail-drop, cycle 4), the fix is one line + a fence, and the owner's standing instruction for this session is to complete the reviewed release; the seal round (7) verifies exactly X30 and nothing else; any further finding stops the release and escalates to the owner |
| 3.0.4 | 9 | triad-sol | X32 | rounds 8-9 ran without a recorded authorization row (X31 covered only the X30 fix + one seal round): r8 was a SAME-SHA re-seal after a packet typo (zero code delta), r9 the rebase seal after the OWNER's own README push (9664e698) reached origin/main mid-release | fixed | owner-session authorization recorded: the owner's standing instruction for this session (fix #20, run the review per flow, release 3.0.4) covers mechanical re-seals that carry NO product delta; AUTHORIZATION.json amended with the explicit extension record; both extra rounds sealed clean on code (r8: zero FAIL; r9: all code PASS) |
| 3.0.4 | 9 | triad-fable | X33 | packet DECLINED_FINDINGS omitted the ledger's fifth declined row (X31) despite claiming machine-regeneration | fixed | DECLINED_FINDINGS regenerated including X31; the regeneration source is the ledger's declined rows, verified by count |
| 3.0.4 | 10-11 | panel-infra | X34 | rounds 10-11 sol/gemini slots flagged "implausible" by the liveness floor on the 2-row accounting delta, while their raw reports contained genuine typed PASS verdicts on every item with correct observed models (13-16s durations) | fixed | owner-session resolution recorded POST-tag (this row): per the protocol's own self-test clause (two identical failures from different models = the PROTOCOL is wrong), the floor misfires on micro-deltas; the attestation seals the round-11 raw reports verbatim (verdict pass), receipt bound to the exact tag SHA 9792b1dc; consider a delta-size-aware liveness floor (BACKLOG candidate) |
| 3.1.0-Ф1 | 1 | triad-sol | X35 | planRun finalizer ignored winner work_state (plan could exit 0 on needs_input) | fixed | wave-1 batch 14e958a3 (planTerminal.ts fold + plan canaries) |
| 3.1.0-Ф1 | 1 | triad-sol | X36 | one-shot continuation wired only in the read-only loop; enveloped candidates never continued | fixed | 14e958a3 (runSlot continuation + canary) |
| 3.1.0-Ф1 | 1 | triad-sol | X37 | deep-scan reducer bypassed the unified finalizer (a fourth deliverable predicate) | fixed | 14e958a3 (envelope threaded via buildSpec; unwrap+finalize) |
| 3.1.0-Ф1 | 1 | triad-sol | X38 | needsDecision admitted accept_risk for work_state-only vetoes (false ACK, gate then refuses) | fixed | 14e958a3 (needsOperatorInput split; decision 409 work_state_needs_input) |
| 3.1.0-Ф1 | 1 | triad-sol | X39 | unconfirmed process death only via an unsupplied optional callback | fixed | 14e958a3 (typed termination_unconfirmed ProcEvent + terminal fact) |
| 3.1.0-Ф1 | 1 | panel-infra | X40 | fable triad+scope slots dead: prompt 943K tokens + 100K output > 1M endpoint cap | fixed | round-3+ run with TRIAD_MAX_PACK_BYTES=1000000 / TRIAD_MAX_OUTPUT_TOKENS=60000; full live coverage from round 3 |
| 3.1.0-Ф1 | 2 | triad-sol | X41 | rankingReason claimed expiring_quota_slack unconditionally for goal=auto | fixed | wave-2 batch 1d6a2a50 (mirror comparator) — superseded by X52's structural fix |
| 3.1.0-Ф1 | 2 | triad-sol | X42 | reverse-clean apply no-op ignored --mode (commit/branch/pr reported false success) | fixed | 1d6a2a50 (no-op restricted to mode=apply; typed refusal otherwise) |
| 3.1.0-Ф1 | 2 | triad-sol | X43 | missing-leader-with-live-group read as confirmed (leader-exit race) | fixed | 1d6a2a50 (unresolved on live-pgid probe; direct-group identity seeded at spawn) |
| 3.1.0-Ф1 | 2 | triad-sol | X44 | plan_not_ready recorded retryable=true though the frozen planRef can never become ready (INV-081) | fixed | 1d6a2a50 (retryable:false + new-Implement-turn remediation) |
| 3.1.0-Ф1 | 2 | triad-sol | X45 | ARCHITECTURE/DEVELOPMENT restated the retired >=2/<=10 review protocol (conflicts with CHECKLISTS v3) | fixed | 1d6a2a50 (W-g pulled forward; single pointer at CHECKLISTS) |
| 3.1.0-Ф1 | 2 | triad-sol | X46 | sealed pack omits full text of some changed files (endpoint cap) | declined | transport limitation: 1M endpoint cap; omissions disclosed in-prompt; full DIFF always present (DECLINED_FINDINGS #1) |
| 3.1.0-Ф1 | 3 | triad-sol | X47 | inactivity-watchdog cleanup grace shorter than the reap proof deadline; early return undisclosed | fixed | wave-3 batch cd2601c2 (drain to REAP_PROOF_DEADLINE_MS; typed unconfirmed surfaced) |
| 3.1.0-Ф1 | 3 | triad-gemini | X48 | candidate errorReason persisted/served unredacted | fixed | cd2601c2 (redactSecrets at the projection); the finding's missing-import-crash half was factually wrong |
| 3.1.0-Ф1 | 3 | triad-gemini | X49 | deep-scan reducer HOME allegedly unbridged for claude (INV-067) | declined | factually wrong: scouts and reducer share the same generic read-only home; the claude ADAPTER applies the Keychain bridge keyed on spec.env.HOME (harness-claude/index.ts:845) identically for both (DECLINED_FINDINGS #2) |
| 3.1.0-Ф1 | 3 | triad-sol+fable | X50 | thread-store errors swallowed to null let produced-root fall back to the LIVE project (fail-open QA-038 edge) | fixed | cd2601c2 (typed authority_unavailable 503; never the live root) |
| 3.1.0-Ф1 | 3 | triad-sol | X51 | economy rankingReason overclaimed subscription_entitlement_first | fixed | cd2601c2 — superseded by X52's structural fix |
| 3.1.0-Ф1 | 3 | triad-fable | X52 | WorkReport locator/description persisted unredacted (INV-062 defense-in-depth) | fixed | cd2601c2 (redactSecrets once in validateWorkReport) |
| 3.1.0-Ф1 | 3 | triad-fable | X53 | CONCEPT-CHANGE(INV-116) marker unverifiable from the packet | fixed | cd2601c2 (CONCEPT-COVERAGE records the e7f20c33/e997099b amendment; concept-gate green range+since-last-tag) |
| 3.1.0-Ф1 | 3 | triad-fable | X54 | BACKLOG still carried the shipped W-h row | fixed | cd2601c2 (row deleted per ship rule) |
| 3.1.0-Ф1 | 3 | triad-fable | X55 | run.continuation / delegation.belt.unavailable / route.pool.degraded had no live text rendering | fixed | cd2601c2 (formatRunEventLine cases) |
| 3.1.0-Ф1 | 3 | triad-sol | X56 | managed-runner PATH prepend lacked realpath/writability hardening | fixed | cd2601c2 (regular-file + not group/world-writable; symlink tests) |
| 3.1.0-Ф1 | 3 | triad-sol | X57 | no targeted secret-scan record in the packet | fixed | cd2601c2 (scan clean over 165 files; SECRET_SCAN.txt sealed into the packet) |
| 3.1.0-Ф1 | 3 | triad-fable | X58 | DECIDED_TRADEOFFS ratchet figure stale vs frozen tree (7325 vs 7483) | fixed | packet source corrected before round 4 |
| 3.1.0-Ф1 | 4 | triad-sol | X59 | raw kill(-pid) remained on spawnProcess cancel paths (PID/PGID-reuse hazard) | fixed | wave-4 batch 89084991 (identity-verified handle only; no-handle=no-signal) + closing residue commit for runCaptureRaw (this commit) |
| 3.1.0-Ф1 | 4 | triad-sol | X60 | reap rescans discovered descendants from a bare rootPid (PID-reuse could enroll an unrelated tree) | fixed | 89084991 (root identity bound; reuse fixture + no-guard control) |
| 3.1.0-Ф1 | 4 | triad-sol | X61 | rankingReason wrong a third time (equal non-null slacks) | fixed | 89084991 (compareAutoTraced: one traced comparator feeds both sort and rationale — structural) |
| 3.1.0-Ф1 | 4 | triad-sol | X62 | Exact Retry collapsed frozen efforts to the primary lane scalar (QA-035 completeness) | fixed | 89084991 (harness-scoped efforts map end-to-end) |
| 3.1.0-Ф1 | 4 | triad-fable | X63 | run.continuation emitted before the lease was reserved (over-disclosure on denial) | fixed | 89084991 (reserve-first; typed run.continuation.denied + canary) |
| 3.1.0-Ф1 | 4 | triad-fable | X64 | routingRationaleByRun leaked for pre-telemetry terminals; triple detail fetch on the CLI terminal path; gate-argv truncation undisclosed | fixed | 89084991 (onSettled cleanup; single fetch + pure projections; omission marker) |
| 3.1.0-Ф1 | recheck | fable-recheck | X65 | runCaptureRaw retained the raw kill(-pid) cancel/timeout path (X59 residue) + stale belt comment | fixed | this commit (identity-verified handle for capture children; object-pinned child.kill fallback; spy tests 17/17) |
| 3.1.0-Ф1 | recheck | fable-recheck | X66 | 89084991 ratchet raises lacked a commit-body justification | fixed | recorded here: cli 1463→1471 (single-fetch refactor), claudexord 582→583, orchestrator 7483→7552 (continuation reserve-first + rationale cleanup), control.ts 1676→1686 (efforts map) — all proportionate to the adjudicated fixes |
| 3.1.0-Ф2 | 1 | triad-sol+fable | X67 | daemonRun failure branches (ensureDaemon/attachments/--resume/terminal catch) bypassed the D-7 projector | fixed | wave-1 batch 3ba62308 (all five branches + stream mode for --json-stream) |
| 3.1.0-Ф2 | 1 | triad-sol | X68 | mcp-runner __runs_list undercounted under the new 200-row default page | fixed | 3ba62308 (cursor walk) → refined in 543d070b (count-only pages, no row accumulation) |
| 3.1.0-Ф2 | 1 | triad-fable | X69 | claude-bridge never reached envelope candidates (worktrees materialize only committed files) | fixed | 3ba62308 (bridge written into each git-mode envelope; marker-gated :(exclude,top) keeps patch.diff clean; reviewer-suggested info/exclude proven inapplicable to linked worktrees) |
| 3.1.0-Ф2 | 1 | triad-sol+fable | X70 | QA-067 semantic-text allowlist too narrow (.js/.ts/.toml/... still binary-path raw) | fixed | 3ba62308 (common code/config set) |
| 3.1.0-Ф2 | 1 | triad-sol | X71 | CLI projector output unredacted (message/fieldErrors/details/context) | fixed | 3ba62308 (one redaction owner) → ordering corrected in 543d070b (redact THEN bound; straddle test) |
| 3.1.0-Ф2 | 1 | triad-sol | X72 | project remove unregistered before the fallible partition archive | fixed | 3ba62308 (archive-first) → hardened in 543d070b (rename-then-close, reopen-on-failure, rollback + project_remove_partial disclosure) |
| 3.1.0-Ф2 | 1 | triad-fable | X73 | usage_error vs invalid_argument code vocabulary split | fixed | 3ba62308 (unified invalid_argument) |
| 3.1.0-Ф2 | 1 | triad-fable | X74 | WHITEPAPER untouched by a public-contract phase; README/INTEGRATIONS duplication; INV-113 wording divergence | fixed | 3ba62308 (whitepaper paragraphs; README pointer; synced wording) |
| 3.1.0-Ф2 | 1 | triad-fable | X75 | packet TESTS.txt vs TEST_RESULTS.json canary-count drift | fixed | reseal generates both from one source; mkpacket phase metadata corrected (FREEZE said F1 in the Ф2 packet) |
| 3.1.0-Ф2 | 2 | triad-sol | X76 | projector stragglers: dispatch default, inspect missing-run, apply unsupported-mode, ops-commands ad-hoc envelopes | fixed | wave-2 batch 543d070b (full sweep + extras passthrough, projector fields win) |
| 3.1.0-Ф2 | 2 | triad-sol | X77 | .env-class artifacts served raw via the binary path (extname('.env')==='') | fixed | 543d070b (typed sensitive_file_refused 409 across all three fetch families, before bytes are read) |
| 3.1.0-Ф2 | 2 | triad-fable | X78 | extend key_required 400s the CURRENT keyless macOS Extend button | fixed | 543d070b (Idempotency-Key optional: bound+replay-safe when present, legacy when absent; catalog honestly `none`) |
| 3.1.0-Ф2 | 2 | triad-fable | X79 | removeProject fence typed too loosely (optional scope could skip the fence) | fixed | 543d070b (RunScope.safeParse; unresolvable live-run scope fails closed) |
| 3.1.0-Ф2 | recheck | fable-recheck | X80 | ops-commands:408 `{ok:accepted, job}` terminal-login-job emit + codeless unknown-command envelope | declined | result-not-error doctrine: a job that STARTED reports its terminal state as a result (docs carve-out); `code` is optional by contract; both noted, neither violates an invariant |
| 3.1.0-Ф3 | audit | codex-max-audit | X81 | D-9/#22 server half missing: daemon accepted quality-routing with zero usable tiers; runtime RoutingPreflightError mapped to harness_unavailable | fixed | 0e7e9145 (POST /settings validates merged effective routing → typed 4xx config_error; one routingFailureClassification helper classifies preflight as config_error across all strategies) |
| 3.1.0-Ф3 | audit | codex-max-audit | X82 | QA-029 dropped (confirmed item in no phase/backlog): generated Claude artifacts advertised unregistered /claudexor + bare-CLI fallback (exit 127) | fixed | db5ef314 (namespaced /claudexor:claudexor + absolute-runtime-path fallback from the validated descriptor); Claude-host version proof → D-22 backlog |
| 3.1.0-Ф3 | audit | codex-max-audit | X83 | claude-bridge excluded a candidate-edited CLAUDE.md from patch.diff by marker SUBSTRING (work loss) | fixed | 48d5dda6 (byte-equality with CLAUDE_BRIDGE_CONTENT, no-follow; independent re-check confirmed FIXED, red-first vs the old substring, safe failure direction) |
| 3.1.0-Ф3 | audit | codex-max-audit | X84 | X65 (runCaptureRaw self-fix, Ф1) had no independent re-check | verified | independent fable re-check confirmed X65 FIXED (no raw -pid residue in runCaptureRaw or spawnProcess; child.kill-on-exited empirically no-op) — bookkeeping, no code change |
| 3.1.0-Ф3 | audit | codex-max-audit | X85 | (worktree, pre-merge) D-2 publish rebuilt the closure instead of promoting exact candidate bytes | fixed | f4a 056c1545 (publish downloads candidate artifact by run id + gh attestation verify + byte-identity; no rebuild) |
| 3.1.0-Ф3 | audit | codex-max-audit | X86 | (worktree, pre-merge) D-15 reuse-lock ignored: duplicate metrics fetcher, allowlist drift vs releaseStats | fixed | f5prep f2d1e081 (one shared repo-asset-authority module; expose-both policy; parity tests) |
| 3.1.0-Ф3 | audit | codex-max-audit | X87 | origin/main advanced to 756b40d1 (#36 Pages, #37 npm) while v310 branches from 24a64be6; pages.yml has mutable action tags; release-workflow-check scans only ci/release.yml | deferred | INTEGRATION-ORDER A-7: merge origin/main before the Ф5 freeze, SHA-pin pages.yml, widen the workflow-check, include site/** in immune scan (recorded in PLAN.md INTEGRATION ORDER + BACKLOG) |
| 3.1.0-Ф3 | audit | codex-max-audit | X88 | reviewer packets omitted 35-100 changed files' current text per round (pack budget); disclosed but not full-context | accepted-corrected | A-8: Ф3+ waves supply the full text of every changed file (Ф3 diff is compact enough); full diff was always present in prior waves |
| 3.1.0-Ф3 | recheck | a3-residual-recheck | X89 | A-3 residual (48d5dda6): byte-equality alone cannot distinguish "ensureClaudeBridge wrote this bridge THIS run" from "a candidate rewrote a PRE-EXISTING committed CLAUDE.md to exactly CLAUDE_BRIDGE_CONTENT"; the latter's real edit was excluded from patch.diff (work loss on the very bug A-3 fixes) | fixed | this packet — WorkspaceManager records the created-this-run fact (ensureClaudeBridge().created, keyed by envelope id, in-memory) and diff() AND-gates the exclusion on it plus byte-equality; a lost fact (pre-existing file, prior run, daemon upgrade mid-run) fails toward CAPTURE; stale marker-verified comments (git.ts, manager.test.ts) updated; red-first test manager.test.ts "A-3 residual: captures a candidate that rewrites a PRE-EXISTING committed CLAUDE.md to exactly the bridge bytes" |
| 3.1.0-Ф3 | gate-1 | triad-sol | X90 | settings updateGlobalConfig validate-then-write TOCTOU: goal/tiers consistency checked outside the mutator raced concurrent writers (A-1 surface) | fixed | 7543065d (commitSettingsUpdate single write owner; assertRoutingGoalTiersConsistent inside the mutator) |
| 3.1.0-Ф3 | gate-1 | triad-sol | X91 | QA-009 regression from the envelope unwrap: orchestrator's typed-final correlation still matched the RAW envelope text, so unwrapped finals broke correlation | fixed | 7543065d (AnswerAssembly.machineText(): raw envelope for machine reads, unwrapped output for display; 4 unwrap sites swapped) |
| 3.1.0-Ф3 | gate-1 | triad-sol | X92 | review-coverage-check parsed `git diff --name-status` without -z (rename/space paths mis-split) | fixed | 7543065d (parseNameStatusZ over NUL records) |
| 3.1.0-Ф3 | gate-1 | triad-sol | X93 | external-open temp staging trusted a pre-existing root (perms/ownership unproven before stage/sweep) | fixed | 7543065d (ensureSecureRoot: 0700 user-owned non-symlink root, fail-closed) |
| 3.1.0-Ф3 | gate-2 | triad-sol, triad-fable, scope | X94 | packet-split full-text pack EMPTY: inPackSubset treats a slash-less selector as an exact-file match, so `apps/macos`/`packages` matched nothing and both sub-waves reviewed diff-only (the exact A-8 hole) | fixed | 226d9282 (zero-match selectors throw loudly) + trailing-slash selector files; round-3 re-ran with full text |
| 3.1.0-Ф3 | gate-2 | triad-sol | X95 | TESTS.txt bound the gate battery to stale candidate bc678255 while FREEZE pinned 7543065d — evidence not bound to the reviewed tree | fixed | packet rebuilt for round-3: battery re-run AT the frozen candidate, TESTS.txt records the same sha as FREEZE.json |
| 3.1.0-Ф3 | gate-2 | triad-sol, triad-gemini, triad-fable | X96 | refreshRuns (QA-052): unbounded `while true` trailing loop could spin under sustained triggers; unconditional `runsRefreshTask = nil` clobbers a reconnect-replaced task | fixed | 2fd77692 (bounded lead + at-most-one trailing pass; Equatable identity guard mirroring the reconnect fence). Gemini's "not @MainActor" leg declined with evidence: class-level @MainActor annotation isolates it |
| 3.1.0-Ф3 | gate-2 | triad-gemini | X97 | claim: attributesOfItem(atPath:) is stat-like (follows symlinks) so the external-open root symlink check is defeated | fixed-with-correction | premise empirically false on macOS 26 (3 probes: reports .typeSymbolicLink); switched to resourceValues(.isSymbolicLinkKey)+lstat anyway as the unambiguous API; tests strengthened to prove lstat-not-stat (symlink → valid 0700 target now refused with the exact "is a symlink" reason) 2fd77692 |
| 3.1.0-Ф3 | gate-2 | triad-fable | X98 | INV-113 stale: Bible still described the claude-bridge exclusion as "exact marker-gated path" (pre-A-3 wording); a reader would think a candidate-edited CLAUDE.md keeping the marker is dropped | fixed | 2fd77692 (byte-equality AND created-this-run wording; ARCHITECTURE §7 gained the created-this-run gate) |
| 3.1.0-Ф3 | gate-2 | triad-sol | X99 | FEATURES.md engine/work-report row stale: said the codex FINAL message keeps the raw envelope (pre-unwrap behavior) | fixed | 2fd77692 (display text = unwrapped output; raw envelope on payload.work_report_envelope; machineText() for the orchestrator) |
| 3.1.0-Ф3 | gate-2 | triad-sol, triad-fable | X100 | DESIGN_SYSTEM outcome-receipts section wrong twice: claimed unknown valuation renders "unknown" and an "executor/review split when known" — neither exists in TurnCardBars | fixed | 2fd77692 (doc corrected to code truth: valuation absent when unknown; no split) |
| 3.1.0-Ф3 | gate-2 | triad-sol | X101 | ComposerOptionParser.parseCommandArgv silently accepts an unterminated quote (malformed test-command sent as if closed) | fixed | 2fd77692 (typed CommandArgvError.unterminatedQuote; Create test-command field surfaces it and blocks Send) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X102 | ArtifactCategory.semanticTextExtensions is a hand-maintained duplicate of the server SEMANTIC_TEXT_EXTENSIONS (QA-067 drift risk; existing Swift test hard-coded the list again) | fixed | 2fd77692 (scripts/artifact-text-parity-check.mjs reads BOTH sources, pins swift == server ∪ MIME_TEXT_GROUP; wired into release:verify:node) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X103 | plugins.ts hostFallbackCommands: String.replace with a string replacement expands `$` patterns, corrupting a $-bearing runtime prefix | fixed | 2fd77692 (function replacement; test with a $&-bearing exec-shim path) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X104 | removeFinalizedTwin compared against `prefix(blockCharCap)` while storage bounds via boundHead — duplicated bounding logic could diverge | fixed | 2fd77692 (shared pure clip() used by both bound and the twin check) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X105 | refreshProjects only ran on connect(): composer nesting hint stale after a mid-session project registration (QA-072 surface) | fixed | 2fd77692 (also refreshes on the coalesced ping-driven thread refetch) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X106 | BudgetSnapshot absent `evidence` → "complete"; ThreadTurnInfo `prompt` demoted to decoded-default — could mask version-skew truth | declined-with-evidence | the server zod wire contract itself declares these defaults (control.ts evidence default "complete"; thread.ts prompt default "") — Swift mirrors the contract; forking it would be the dishonest move. Comment clarified 2fd77692 |
| 3.1.0-Ф3 | gate-2 | triad-fable | X107 | envelopeActive FINAL with output:"" was dropped by the `!ev.text` guard — the machine envelope (payload.work_report_envelope) was lost with it | fixed | 2fd77692 (an empty-display final carrying an envelope is kept: display empty, machineText() returns the envelope, hasFinal() true) |
| 3.1.0-Ф3 | gate-2 | triad-fable | X108 | turbo globalPassThroughEnv DEVELOPER_DIR is excluded from task hashing (native build not cache-keyed on it) | declined-with-evidence | intended pass-through semantics for a dev-env var; JSON cannot carry the suggested comment; blast radius of restructuring > advisory value |
| 3.1.0-Ф3 | gate-2 | triad-fable | X109 | refreshRuns doc comment claimed GET /v2/runs returns the WHOLE retained registry (O(total runs)); the server serves a bounded newest-first page (run-list.ts default limit + hard cap) | fixed | doc comment corrected line-neutral (this commit); single-flight rationale intact |
| 3.1.0-Ф3 | gate-2 | triad-sol | X110 | ComposerOptionParser advisory leg (sol): malformed syntax should be a typed parse error, not silent acceptance — same root as X101 | fixed | folded into X101 (2fd77692) |
| 3.1.0-Ф3 | gate-2 | self (full-chain battery) | X111 | wave-2 fix batch touched CLAUDEXOR_BIBLE.md (INV-113 wording) without a CONCEPT-CHANGE marker; the fix agent ran gates as a hand-picked list that omitted concept-gate, so it passed locally and failed only in the full release:verify:node chain | fixed | c6140737 (append-only CONCEPT-COVERAGE ratification of 2fd77692/3ba62308/af8ff86a — all INV-113 refinements of the owner-ratified A-3 mechanism); standing guardrail: agents run the FULL chain, never a subset |
| 3.1.0-Ф3 | gate-3 | triad-sol | X112 | performRunsRefresh rebuilt TaskRun from list summaries preserving only SOME hydrated fields — a loadRunDetail → refreshRuns sequence wiped valuationUsd/council/outcomeBanner/applyEligibility/planReadiness/planQuestions receipts | fixed | 0d32e4e4 (every detail-only projection preserved when the summary carries no replacement; survival test) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X113 | diff/artifact failure projections dropped the artifact PATH (RunDiffSection showed only a run id; failure sheet only a basename) | fixed | 0d32e4e4 (canonical path carried in RunDiffLoadOutcome.failed/PayloadError; 413+transport tests assert visibility) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X114 | producedTextOutcome used lossy String(decoding:) — malformed UTF-8 displayed as corrupted text instead of an honest non-renderable failure | fixed | 0d32e4e4 (strict decode; nil → .notRenderable("<path> is not valid UTF-8 text"); malformed fixture) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X115 | the sealing contract could not represent a packet-split panel: validateReviewPanelCoverage permitted exactly one triad+scope, no coverage receipt existed, so sealing ONE sub-wave could omit the others undetected | fixed | 5ec7d49f (attestation schema v4: per-sub-wave full panels via triad@subwave=model; signature-bound coverageReceipt from review-coverage-check --receipt REQUIRED for packet-split; 6 contract tests). Enforcement bound at the SEAL (verify-time has no packs) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X116 | packet narratives (USER_INTENT/PHASE_DELTA) still hand-named candidate bc678255 while FREEZE bound c8b45ca9 — same class as X95 | fixed | mkpacket now refuses any narrative "candidate <sha>" not prefix-matching the frozen candidate; narratives rewritten candidate-free (FREEZE is the only authority) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X117 | SECRET_SCAN.txt copied stale (bound to 7543065d/104 files vs frozen 108) | fixed | mkpacket GENERATES the targeted scan at packet build over the exact frozen base..candidate (containsSecretLikeToken over diff-added lines + full current text) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X118 | WHITEPAPER lacked the era-neutral rationale for the D-9 routing config_error semantics (checklist requires it for routing-policy changes) | fixed | b355175c (Routing Is Not Strategy: absent comparable tiers = configuration the user completes, not a harness to wait on) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X119 | candidate-path answerText lacked redactSecrets symmetry with read-only/deepScan unwrap sites (INV-062) | fixed-with-evidence | b355175c: redactHarnessEvent PROVABLY deep-redacts payload values upstream (JSON stringify→redact→parse, runSupport.ts; new test) so no live leak existed; symmetric redactSecrets added as defense-in-depth + end-state answer.md test |
| 3.1.0-Ф3 | gate-3 | triad-fable | X120 | assertSettingsPatchValid's optional `current` let a future writer bypass the D-9 merged-effective invariant with no compile-time signal | fixed | b355175c (current REQUIRED; unconditional enforcement; 6 test callers updated) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X121 | triad transport enumerated changed files via newline --name-only while the coverage gate parsed -z --name-status — a space/unicode path would be C-quoted into a false "(deleted)" pack entry (fail direction safe: the checker blocks) | fixed | 5ec7d49f (transport imports parseNameStatusZ; ONE enumeration owner) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X122 | coverage checker verified only base..candidate changed files — a FILES_TO_READ_WHOLE (listed-but-unchanged) context file outside every sub-wave selector silently lost full-text review (v3.0.1 r6 class; not live this wave) | fixed | 5ec7d49f (--whole-file-list unions packet entries into the required set; test proves a missing listed file now fails the gate) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X123 | THREE hand-maintained copies of the semantic-text extension set (server, Swift, and the Swift test's hardcoded list) — the X102 parity gate pinned only two | fixed | 0d32e4e4 (test folded to representative membership; the parity script is the one cross-source owner) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X124 | tokenizeArgv silently dropped a terminal backslash (escape=true at EOF unchecked) — Create test-command sent a different argv than typed | fixed | 0d32e4e4 (CommandArgvError.danglingEscape; strict parser rejects; surfaced in the Create field) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X125 | performRunsRefresh had no client/generation re-check after await — a response completing concurrently with reconnect could assign liveTasks from the OLD client | fixed | 0d32e4e4 (generation+client identity captured before request, re-checked with Task.isCancelled before mutation; delayed-response test) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X126 | composerHint said budget cap must be "positive" while the typed-money contract accepts finite zero | fixed | 0d32e4e4 (non-negative wording) |
| 3.1.0-Ф3 | gate-3 | triad-sol | X127 | assertSecureRoot validated dir/symlink/uid but not MODE — a pre-existing user-owned 0755 external-open root was accepted despite the documented private-0700 contract | fixed | 0d32e4e4 (st_mode & 0o077 == 0 fail-closed; 0755 pre-existing root test) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X128 | refreshRuns doc comment overclaimed "every caller awaits the coalesced result" — a caller folded during the LEAD pass returns before the trailing pass lands its trigger | fixed | 0d32e4e4 (comment tells the folded-caller truth; bounded design unchanged per X96) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X129 | loadRunDiff conflated engine-offline/row-missing/hydrated into .loaded — offline + terminal run with a patch artifact rendered an EMPTY Changes tab instead of honest failure+Retry | fixed | 0d32e4e4 (client==nil with a patch artifact → typed offline failure) |
| 3.1.0-Ф3 | gate-3 | triad-fable | X130 | QA-065 display names never fired by default: tryConnect refreshed everything EXCEPT credential profiles, so the sessions footer showed raw profile ids until the accounts popover loaded them | fixed | 0d32e4e4 (refreshCredentialProfiles on the connect path) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X131 | INV-125 still defined ONE exact panel reading one sealed packet while CHECKLISTS/contract authorized packet-split — the constitution lagged the mechanism | fixed | e93f5a13 (INV-125 constitutionalizes packet-split: per-sub-wave full panels + sealer-recomputed union receipt; CONCEPT-CHANGE(INV-125), owner basis Разбор-2=a) |
| 3.1.0-Ф3 | gate-4 | triad-sol, triad-fable | X132 | coverage receipt required only when >1 named sub-wave — sealing exactly ONE named sub-wave (a subset by construction) passed with no union proof, the exact X115 evasion; the 1-named case was untested | fixed | e93f5a13 (required when >=1; single-named-without-receipt rejection test) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X133 | sealer trusted a caller-supplied receipt's ok:true and digest-shaped fields — a hand-authored JSON receipt passed the self-check | fixed | e93f5a13 (bindCoverageReceipt: the sealer RE-RUNS coverage over the receipt's referenced packs against the sealed candidate, recomputes pack digests from disk, embeds only recomputed values; forged-receipt git-fixture test) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X134 | nothing mapped a named subWave to the pack digest its reviewers actually consumed — reviewer reports from one set of prompts could seal with a receipt over different packs | fixed | e93f5a13 (receipts map subWave→sha256 via --pack subwave=path; validator requires label/panel set-equality both directions) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X135 | ARCHITECTURE.md + sealer header still described schemaVersion 3 and either-schema acceptance | fixed | e93f5a13 (both tell the v4 + archival-only story) |
| 3.1.0-Ф3 | gate-4 | self (wave transport) | X136 | engine sub-wave prompt hit the 1M-token endpoint cap (1,097,001 tokens) — fable + scope slots died HTTP 400, wave unusable | fixed | engine subset split (packages/orchestrator/ alone vs the rest): three named sub-waves from round 5; partition proven 63+6+46=115 |
| 3.1.0-Ф3 | gate-4 | triad-sol | X137 | neutralizeAbsentOptionalKey recognized absence only for source "api_key_env" while codex's API-key fallback uses provider_auth_file — default codex subscription rendered a RED stored_key failure (QA-005 violation) | fixed | c7e1254b (typed source predicate; codex fixture: absent→neutral, available+failed→red) |
| 3.1.0-Ф3 | gate-4 | triad-sol, triad-fable | X138 | GatewayClient.artifactText still decoded lossily (X114 sibling lane) — run-tree text artifacts could display as valid-looking corrupted evidence | fixed | c7e1254b (strict decode + typed error; malformed-byte test on artifactTextOutcome) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X139 | listRunArtifacts/listProducedFiles projected a malformed 200 as an honest EMPTY gallery ((try? decode) ?? []) — INV-021 violation | fixed | c7e1254b (typed GatewayError.decoding propagated; gallery renders failed+Retry; malformed-200 tests both endpoints) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X140 | refreshProjects silently kept registeredProjects on a failed GET while its comment claimed failure leaves nesting undisclosed — stale overlap shown as current truth | fixed | c7e1254b (typed failure; stale nesting disclosed, not presented as fresh; failed-refresh test) |
| 3.1.0-Ф3 | gate-4 | triad-sol | X141 | the steady-state health loop discarded the handshake engine identity — a daemon replaced at the same endpoint kept the old version/SHA in About indefinitely | fixed | c7e1254b (identity refreshed from each successful handshake; changing-identity test) |
| 3.1.0-Ф3 | gate-4 | triad-fable | X142 | mergeRefreshedTask (X112 fix) omitted the two SSE-only fields: attentionNote (rotation/diverged disclosures wiped mid-run) and retryStatus (W-C2 api_retry note) | fixed | c7e1254b (both preserved; X112 survival test extended) |
| 3.1.0-Ф3 | gate-4 | triad-fable | X143 | AboutInfo (D-11 SSOT) lived at the bottom of AppModel+CredentialProfiles.swift — wrong owner for owner-locked strings | fixed | c7e1254b (pure relocation to its own file) |
| 3.1.0-Ф3 | gate-4 | triad-fable | X144 | packet narratives drifted again outside the X116 guard: RELEASE_PREFLIGHT said "PHASE Ф1", RUNTIME hand-wrote "172 tests" (frozen candidate has 190) | fixed | mkpacket stamps the phase from PACKET_PHASE; RUNTIME defers counts to TEST_RESULTS.json (machine-recorded only) |
| 3.1.0-Ф3 | gate-5 | triad-sol | X145 | liveTask/performRunsRefresh ignored RunSummary.result and the Apply gates keyed on a stale detail-only satellite — CLI apply/revert left macOS showing an eligible Apply (INV-093) | fixed | 244e8a07 (result projected; merge prefers summary.result; both apply gates also require applyState==not_applied; applied+reverted survival test) |
| 3.1.0-Ф3 | gate-5 | triad-sol | X146 | resolvedRunningEngineVersion ignored the retained handshake identity — older serving daemon + newer app yielded a false "Up to date" | fixed | 244e8a07 (handshake version preferred; 3.1.0-engine/3.1.5-manifest test) |
| 3.1.0-Ф3 | gate-5 | triad-fable | X147 | enterHardOffline kept credentialProfiles/harnessAccounts as current truth while offline (X140 class) | fixed | 244e8a07 |
| 3.1.0-Ф3 | gate-5 | triad-gemini | X148 | scheduleThreadsRefresh nil'd its task ref BEFORE the awaited refresh — a mid-await trigger spawned concurrent duplicate refreshes (verified) | fixed | 244e8a07 (ref cleared after completion; coalesced follow-up preserved) |
| 3.1.0-Ф3 | gate-5 | triad-gemini | X149 | stage() safeName guarded empty/./.. but not "/" (lastPathComponent("/") is "/") | fixed | 244e8a07 (+ sanitization test) |
| 3.1.0-Ф3 | gate-5 | triad-fable | X150 | neutralizeAbsentOptionalKey treated api-key-PRIMARY families (opencode/raw) as optional-fallback — a failed stored_key would be wrongly neutralized (not reachable in default config) | fixed | 244e8a07 (guarded on subscription-native default; opencode red test) |
| 3.1.0-Ф3 | gate-5 | triad-fable | X151 | X119's redaction fix trimmed the persisted candidate answer, violating the verbatim contract; a SECOND trim at the write site re-trimmed final/answer.md | fixed | 244e8a07 (redact-without-trim, trim only as emptiness check; both trims removed; whitespace-survival test proven load-bearing) |
| 3.1.0-Ф3 | gate-5 | triad-fable | X152 | plan lane was the one unwrap site without redactSecrets symmetry (upstream deep redaction proven — defense-in-depth) | fixed | 244e8a07 |
| 3.1.0-Ф3 | gate-5 | triad-fable | X153 | resolveHarnessBinary's injected platform seam half-plumbed: binaryNameCandidates read ambient process.platform | fixed | 244e8a07 (win32-candidates-under-injection test) |
| 3.1.0-Ф3 | gate-5 | triad-sol | X154 | the sealer trusted CLI prose for panel identity: nothing verified a report's candidate/model/liveness/verdict — a foreign report could seal as a panel slot | fixed | 888dbd6b (panel slots seal ONLY via typed --slot-record wave metadata: derived verdict, recomputed report digest, candidate/tree/observed-model/wave-id/packet checks; transport records panel_slot/sub_wave/verdict/report_sha256) |
| 3.1.0-Ф3 | gate-5 | triad-fable | X155 | bindCoverageReceipt recomputed against the CALLER'S receipt.base — base≈candidate made the changed set empty and ok:true vacuous (X133 leg) | fixed | 888dbd6b (base from the packet's FREEZE.json via required --packet; base==candidate refused; FILES_TO_READ_WHOLE is packet authority a receipt cannot omit; triad prompt digests signature-bound to receipt pack digests) |
| 3.1.0-Ф3 | gate-5 | triad-sol, triad-fable | X156 | advisories batch: omitted reachable claim was no INV-139 gap; pack budgets counted UTF-16 units; omission-note reasons prefix-matched; deletions vanished from the prompt list; CHECKLISTS described the retired --name-only enumeration + an impossible selector example | fixed | 888dbd6b |
| 3.1.0-Ф3 | gate-5 | triad-fable | X157 | thrown typed problem codes were stamped internal_error at both catch sites (BACKLOG N1 — config_error never reached the wire); the created-this-run bridge fact died with the manager instance | fixed | 888dbd6b + follow-up (thrownProblemCode in problem-response; envelope-id-bound bridge-created.json marker; boundary + manager-recreation tests) |
| 3.1.0-Ф3 | gate-5 | self (transport) | X158 | engine-pkg sub-wave triad slots died on a transient network fetch failure; retried same candidate/pack (scope survived and its findings were adjudicated) | fixed | round-5 retry protocol; sub-waves now carry required --sub-wave names for the seal binding |
| 3.1.0-Ф3 | gate-6 | triad-sol | X159 | cancel landing WHILE the deep-scan reducer ran was never re-checked — the run wrote final/report.md and terminalized succeeded (INV-116; reachable via ask --deep-scan + cancel) | fixed | 2dd3937e (post-synthesis signal re-check routes to the cancelled terminal; reducer classifies an outer-signal abort as typed cancellation discarding partial output; 2 INV-116 tests) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X160 | thread-aggregated gallery committed .loaded(combined) discarding anyFailed — one run's transient listing failure silently omitted its outputs with no disclosure/Retry | fixed | 2dd3937e (failedRunIds state + partial-failure banner naming runs with Retry over the intact snapshot; pure aggregate seam, 4 tests) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X161 | sealer: --packet optional made the manifest binding evadable by omission | fixed | 157734fe (--packet REQUIRED with any --slot-record) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X162 | sealer never checked record.model_id against the frozen panel — an off-panel record could occupy a slot | fixed | 157734fe (validateSlotRecord: model_id==requested==observed AND frozen-panel membership per slot; contract-owned + unit tests) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X163 | sealer accepted status=responded without any liveness validation | fixed | 157734fe (transport records live + liveness_floor_ms; validator requires live===true and duration>=recorded floor) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X164 | duplicate sub-wave labels allowed — ambiguous slot↔pack binding | fixed | 157734fe (refused in receipt validator AND checker CLI) |
| 3.1.0-Ф3 | gate-6 | triad-sol | X165 | CHECKLISTS still instructed the retired --review triad@subwave syntax | fixed | 157734fe (attestation bullet teaches --slot-record + --packet + labeled receipt) |
| 3.1.0-Ф3 | gate-7 | triad-sol | X166 | interrupted context-exhausted candidate (no completed WorkReport) computed errored=false and flowed into review/arbitration/adoption as clean — the D-16 veto elevated only contract_failure | fixed | df53e48c (CandidateRun.outcomeClass propagated; ONE partitionCandidates owner excludes interrupted partials from the reviewable set and terminalizes interrupted/context_capacity_exhausted when no candidate survives; clean continuation still supersedes; 2 gated tests + D-16 canary) |
| 3.1.0-Ф3 | gate-7 | triad-sol | X167 | gallery refresh with a NONEMPTY snapshot + all listings failing returned early without recording failure — stale artifacts as fresh, no disclosure (X160 follow-on edge) | fixed | 7c6690ba (pure loadDecision seam; "Could not refresh — showing last-known results" banner + Retry; 5 tests) |
| 3.1.0-Ф3 | gate-7 | triad-sol | X168 | diff loader keyed on phase.isTerminal — running→unknown (stream loss) never retriggered; indefinite bare spinner with no reason/Retry | fixed | 7c6690ba (key on phase.rawValue; unknown+no patch → typed lost-engine failure with Retry; 9 tests) |
| 3.1.0-Ф3 | gate-7 | triad-sol | X169 | sealer raw-read FREEZE.json: an altered baseSha beside the authentic manifest text passed — manifest ENTRIES were never verified | fixed | 6936af51 (verifySealedEvidencePacket is the only packet authority: every entry hashed, FREEZE candidate/tree bound, verified baseSha feeds bindCoverageReceipt) |
| 3.1.0-Ф3 | gate-7 | triad-fable | X170 | Swift decoded the 409 refusal body with a wire shape the daemon never serves (top-level error/sensitiveClass vs ControlProblem message/context) — both typed branches dead; fixtures had hand-authored the wrong body | fixed | 3ee491f5 (decode mirrors ControlProblem; fixtures repointed at the real normalized body; legacy/hostile shape falls back generically with an anti-resurrection test) |
| 3.1.0-Ф3 | gate-8 | triad-sol, triad-fable | X171 | interrupted veto incomplete: synthesis push, convergence loop, and read-only scouts all bypassed it — an interrupted attempt could converge/synthesize a partial diff as clean; fallback facts claimed noChanges over a real partial diff | fixed | 75649e5a (ONE veto owner across every lane; diff-aware facts; envelope delivery withholds the applyable product from an interrupted run; convergence+unit tests). Salvaged from the spend-limit-killed agent's worktree, completed by lead |
| 3.1.0-Ф3 | gate-8 | triad-sol | X172 | sealed packet AUTHORIZATION.json still said "<=5 fix cycles then escalate" while the ledger recorded cycles 6-7 — the packet under-recorded the owner's re-authorization | fixed | mkpacket AUTHORIZATION waveDiscipline records the owner's 2026-07-24 acceleration directive verbatim (criticals fixed, advisories backlogged, any new critical still blocks) |
| 3.1.0-Ф3 | gate-8 | triad-sol | X173 | sealer trusted the record's verdict claim; --rounds was CLI prose | fixed | 5dc59b4f (deriveSlotVerdict re-parses digest-bound raw bytes; rounds = max(record.round), disagreeing --rounds refuses) |
| 3.1.0-Ф3 | gate-8 | triad-gemini | X174 | O(N²) char-by-char revert-detail trim over megabytes of git stderr starved the daemon event loop | fixed | 5dc59b4f (prefix-slice then <=3-step byte alignment) |
| 3.1.0-Ф3 | gate-8 | triad-gemini | X175 | claim: Task == comparison and stat() shadowing are Swift COMPILER ERRORS blocking the build | declined-with-evidence | the full battery builds and runs green at the same candidate (App 267/40 suites, SWIFT_EXIT=0): Task is Hashable (== legal); stat() resolves to the struct initializer. Both claims refuted by the executed build the reviewer's own packet records |
| 3.1.0-Ф3 | gate-8 | self (coverage gate) | X176 | canary/harness-fake changes from the r7 veto fix fell outside every sub-wave selector — caught by the union coverage gate, not a reviewer | fixed | subsets extended (packages/canary/, packages/harness-fake/); receipt re-proven |
| 3.1.0-Ф3 | gate-9 | triad-sol | X177 | PLAN lane bypassed the D-16 veto: an interrupted/vetoed planner (partial text, no completed report) returned success and became final/plan.md; council merge same path | fixed | 67c0c3c7 (PlannerAttemptOutcome.outcomeClass; interrupted/veto elevate to honest failure; plan terminal projects interrupted/context_capacity_exhausted; test + event-order assert) |
| 3.1.0-Ф3 | gate-9 | triad-sol, triad-fable | X178 | the r8 delivery gate silenced final/summary.md + output.ready for interrupted envelope runs, breaking the documented output.ready-before-terminal contract; work_product.emitted fired with no product written | fixed | 67c0c3c7 (diagnostic summary + output.ready for interrupted; honest work_product.emitted; ordering asserts added to the r8 test) |
| 3.1.0-Ф3 | gate-9 | triad-sol | X179 | WHITEPAPER still described marker-only bridge exclusion — the exact data-loss mechanism A-3 replaced (public trust-model contradiction) | fixed | 67c0c3c7 (created-this-run AND byte-equality wording) |
| 3.1.0-Ф3 | gate-10 | triad-gemini | X180 | claim: runPlannerAttempt references undeclared readonlyWorkMode → ReferenceError in plan mode | declined-with-evidence | readonlyWorkMode exists only in the read-only lane (orchestrator.ts:6779/6977); the planner uses planWorkMode (5739/5845); the full green battery compiles and runs plan-mode tests at this candidate. Same refuted-by-executed-build class as X175 |
| 3.1.0-Ф3 | seal | owner-directed | X181 | Ф3 SEALED at 78b3f3301caffb0f6cc879f0abca1cd770343432 (tree ac9983fb) by owner directive 2026-07-24 («хватит раундов, давай дальше»): 10 gate rounds, X90-X180; final full battery NODE_CHAIN_EXIT=0 / SWIFT_EXIT=0 (2197 node, Kit 193, App 267); macos clean r8, engine-api clean r9, union coverage receipts ok:true each round; residual round-10 engine sub-wave outputs recorded to backlog only | sealed | F3_SHA=78b3f330; remaining advisories in the Ф5 backlog batch |
| 3.1.0-Ф4Ф5 | wave-1 | triad-fable, triad-sol | X182 | D-2 install: single-shot handshake fired immediately after the DETACHED daemon relaunch — nil read as mismatch, EVERY real one-click install spuriously rolled back (masked by instant test stubs) | fixed | 88368874 (bounded 0.5s/30s handshake poll, injectable cadence; mismatch only on wrong version or timeout; delayed-ready + timeout tests) |
| 3.1.0-Ф4Ф5 | wave-1 | triad-sol | X183 | D-2 rollback: try? swallowed stop/restore/relaunch failures and ALWAYS emitted .rolledBack — could report recovery over a broken pointer or dead daemon | fixed | 88368874 (verified RollbackOutcome per step; .failed with exact step+remediation; 4 recovery-failure tests) |
| 3.1.0-Ф4Ф5 | wave-1 | triad-sol | X184 | D-2 post-install: stale pre-install engineIdentity + 304 cachedDecision kept advertising Install after a verified success | fixed | 88368874 (recordInstalledVersion recomputes decision + clears ETag; success → .upToDate directly) |
| 3.1.0-Ф4Ф5 | wave-1 | triad-sol | X185 | D-17: completion handler installed AFTER awaiting login/start — a synchronous response+completion turn lost the completion; default codex device login hung to timeout despite vendor success | fixed | 683fd4ad→1d66ae47 (buffer-before-send + replay; idempotent settle; synchronous-race test) |
| 3.1.0-Ф4Ф5 | wave-1 | triad-sol | X186 | D-16: non-string envelope output coerced String(output) — {"output":{}} delivered "[object Object]" and could finalize CLEAN on the validated cursor path | fixed | 683fd4ad→1d66ae47 (schemaless output MUST be a string; wrong type → work_report_contract violation; 8 channel tests) |
| 3.1.0-Ф4Ф5 | wave-1 | self (transport) | X187 | wave transport: 2MB asset diff blew the 1M-token window (binary base64 blobs in every prompt); meta pack tried to carry 500KB PNGs as full text | fixed | 5227e480 + readable-diff-view commit (prompt diff excludes binary/generated bulk, listed by path; binary media classified diff-authoritative by the ONE shared rule; sealed DIFF.patch stays the full truth) |
| 3.1.0-Ф4Ф5 | wave-1 | panel (advisories) | X188 | (superseded per protocol: one finding = one row) expanded into X192+ below | recorded | each advisory has its own row; backlog holds the work items |
| 3.1.0-Ф4Ф5 | wave-1 | openai/gpt-5.6-sol | X192 | [runtime_behavior_changes] apps/macos/ClaudexorApp/Sources/ClaudexorApp/AppRuntimeDaemonControl.swift, runNodeJSON(_:node:timeout:): the documented hard timeout only schedules Process.terminate(), then blocks in readDataToEndOfFile() and waitUntilExit(). A child that ignores SIGTERM, or a descendant retaining stdout, can block the installer indefinitely; there is no bounded KILL fallback. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | openai/gpt-5.6-sol | X193 | [security_and_secrets] apps/macos/ClaudexorApp/Sources/ClaudexorApp/RuntimeInstaller.swift, unpack(_:version:): the signed tarball is passed directly to /usr/bin/tar without validating entry paths, hard links, or symlink targets. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X194 | [review_protocol] scripts/triad-scope-review.mjs (candidate commit 805095d9, loadFrozenPacket): the reviewer prompt's operative diff now excludes packages/schema/generated (alongside site/assets, docs/assets, pnpm-lock.yaml) and lists them only by path in the 'Diff view note'. Under the A-8 coverage model those generated schema files are classified DIFF-AUTHORITATIVE — i.e. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X195 | [runtime_behavior_changes] apps/macos/ClaudexorApp/Sources/ClaudexorApp/RuntimeInstaller.swift, RuntimeInstallError.daemonBusy errorDescription: "The engine is busy running jobs; the update will retry when idle." is dishonest — nothing retries automatically. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X196 | [runtime_behavior_changes] apps/macos/ClaudexorKit/Sources/ClaudexorKit/SetupLifecycleController.swift: performAction() and adoptAndObserve() call publish(...) WITHOUT passing deviceCode (the parameter defaults to nil), so pressing 'Extend login wait (15 min)' (or any other action) while a codex device-code login is awaiting_user momentarily clears SetupLifecycleSnapshot.deviceCode — AuthSheetDeviceCodeCard disappears until the next snapshot fence in observe() restores it. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X197 | [runtime_behavior_changes] apps/macos/ClaudexorApp/Sources/ClaudexorApp/RuntimeInstallCoordinator.swift install(): failure paths at steps 5/6 (probe mismatch, busy) clean up with removeVersionDir, but a throw from step 7 (`try await daemon.stop()`), the pointer-write catch, the relaunch-throw path, and the handshake-mismatch rollback all leave the freshly unpacked, quarantine-stripped versions/<v> directory on disk with no pointer referencing it. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X198 | [forgotten_touchpoints] .github/workflows/repo-metrics.yml pauses the daily cron 'for the v3.1.0 release freeze' and says RE-ENABLE is the 'release runbook final step', but docs/CHECKLISTS.md — per CLAUDEXOR_BIBLE.md the SOLE home of the release protocol — is not touched anywhere in this diff, so the re-enable step exists only as a workflow comment and can be silently forgotten after publish. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X199 | [prompt_doc_sync] README.md Metrics section states the charts are 'refreshed daily by a scheduled workflow that commits the charts back into the repo', but .github/workflows/repo-metrics.yml ships with the schedule COMMENTED OUT for the freeze (workflow_dispatch only) — until the runbook re-enable happens, the public README describes a cadence that is not running. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X200 | [prompt_doc_sync] docs/FEATURES.md engine/delegation row ('--delegate belt in the PACKAGED macOS app, QA-024') still carries Planned='Ф4' and prose 'Making the packaged app actually HOST the belt ... is Ф4 packaging work', but Ф4 is this very release (D-2 + D-17 per USER_INTENT.md) and the belt packaging did not ship — the pointer is now stale. Retarget the row's Planned column (e.g. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X201 | [implicit_contracts] GatewayClient.engineHasActiveWork (ClaudexorKit/GatewayClient.swift) hardcodes the state-filter values ["running", "queued"] for GET /v2/runs, but the daemon's `state` query is STRICT ('a typoed or malformed value is a typed 400' per the ARCHITECTURE run-list contract / packages/control-api/src/run-list.ts), and the only tests (RuntimeBusyGateTests) run against BusyStubURLProtocol, never the real enum. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | openai/gpt-5.6-sol | X202 | [runtime_behavior_changes] packages/cli/src/claudexord-probe.test.ts imports runProbeIfRequested from packages/cli/src/claudexord.ts, but claudexord.ts unconditionally invokes main() at module evaluation. Running this focused test therefore also starts durable daemon initialization, writer-lease acquisition, journal/setup services, and socket/control startup in the test worker. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X203 | [runtime_behavior_changes] packages/cli/src/claudexord.ts `runStopIfRequested` (the identity-proven `claudexord --stop` the macOS RuntimeInstallCoordinator drives before the atomic pointer swap) ships with NO test at the package boundary: packages/cli/src/claudexord-probe.test.ts covers only `runProbeIfRequested`, and the Swift AppRuntimeDaemonControlTests exercise a fake script, not this TS logic. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X204 | [runtime_behavior_changes] packages/cli/src/setup-login-inline.ts `TerminalLoginNextAction` (the typed `--json` nextAction for a device_auth_unsupported miss) carries only {kind, reason, loginFlow} and omits the profile target: for `claudexor profiles login codex <id> --json`, a machine consumer that follows nextAction verbatim would construct a DEFAULT-STORE browser_redirect login instead of the profile-scoped one (the TTY path handles this correctly via createTerminalFallbackJob's profileId, and the enclosing job object does carry profileId, but the typed action itself is incomplete). | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X205 | [runtime_behavior_changes] packages/cli/src/repo-asset-authority.ts `parseCsv` performs no validation: a malformed or hand-edited ledger row (missing columns, non-numeric field) yields Number(undefined)=NaN which then propagates silently into `prior.npm_total + delta` in scripts/update-repo-metrics.mjs, poisoning the cumulative npm_total for every subsequent day, contrary to the module's own 'never poison the count' doctrine (which is enforced only on the network-payload side). | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X206 | [prompt_doc_sync] docs/DEVELOPMENT.md's owner signing-ceremony example is a broken shell snippet: the line '--in runtime-manifest.json # the candidate's unsigned manifest' carries an inline comment and NO trailing backslash, so the multi-line 'pnpm sign:runtime-manifest' command terminates after --in and the following '--sha256 ...' lines run as separate commands. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X207 | [cross_module_bugs] Authorization-header inconsistency in the new D-17 CLI stream: setup-login-inline.ts's default fetchImpl calls controlApiFetch(addr, path, init) with NO Authorization header (the snapshot GET passes no headers at all; createTerminalFallbackJob passes only Content-Type), while EVERY sibling production call site in this same sub-wave explicitly adds 'Authorization: Bearer addr.token' (daemonGet and gcCommand in ops-commands.ts, the setup-job create in credential-commands.ts, secrets/profiles PATCH/DELETE). | SUPERSEDED-declined | docs/BACKLOG.md §Ф4+Ф5 — post-release audit: this row contradicts X191/X226/X234, which proved controlApiFetch (packages/cli/src/live.ts:317) sets Authorization on EVERY call; the row stays for history but must NOT be actioned |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X208 | [implicit_contracts] The --json failure-envelope contract (ARCHITECTURE 'Design constraints': every FAILURE class is normalized by the ONE projector in packages/cli/src/cli-error.ts into {ok:false, exitCode, code?, message, ...}) is bypassed by new code: setup-login-inline.ts streamDurableCodexLogin in --json mode prints ad-hoc failure objects — {ok:false, error:'snapshot_unavailable', status, jobId} lacks both 'message' and 'exitCode', and the not_supported terminal object {ok:false, job, nextAction} likewise carries no canonical envelope fields — so a machine consumer keying on the documented projector shape gets an unrecognized failure form on the auth-login path. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | openai/gpt-5.6-sol | X209 | [security_and_secrets] packages/schema/src/setup.ts, ControlSetupJobEvent and ControlSetupJobSnapshot: deviceCode is an unconstrained optional field. The schemas accept and serialize a one-time userCode for a non-Codex job, a terminal job, or a phase other than awaiting_user, despite the contract saying this sensitive disclosure exists only transiently while Codex awaits the user. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | openai/gpt-5.6-sol | X210 | [security_and_secrets] packages/secrets/package.json, description: the published metadata claims "OS keychain where available, else a 0600 file", while the current architecture and DEVELOPMENT.md explicitly state that the managed secret store is file-only and has no System Keychain backend. This is misleading public security metadata in the touched 3.1.0 package release. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X211 | [review_protocol] scripts/triad-scope-review.mjs (candidate commit 805095d9, loadFrozenPacket): the new READABLE diff view excludes 'packages/schema/generated' bodies from the reviewer prompt, but the deterministic coverage checker (per the packet-split contract) classifies exactly those files as DIFF-AUTHORITATIVE — i.e. files whose review channel IS the diff and which are exempt from full-text pack coverage. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X212 | [runtime_behavior_changes] packages/util/CHANGELOG.md: the 3.1.0 entry is completely empty ('## 3.1.0' with no bullets) even though this release adds an entire new public API surface to @claudexor/util — packages/util/src/runtime-manifest.ts (RUNTIME_UPDATE_AUTHORITY, SignedRuntimeManifest, verifyRuntimeManifest, isMonotonicRuntimeUpgrade, canonicalJson, runtimeArchiveUrl) re-exported from index.ts and consumed by the CLI's fail-closed release check. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X213 | [forgotten_touchpoints] docs/CHECKLISTS.md — the Bible-designated sole home of the release protocol (INV-130 hierarchy) — is NOT in the changed-file list, yet the publish flow gained two new mandatory operator inputs (release.yml runtime_manifest_b64 + candidate_run_id, validated fail-closed in the prepare job) and .github/workflows/repo-metrics.yml explicitly defers its schedule re-enable to a 'release runbook final step'. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | wave-1 | anthropic/claude-fable-5 | X214 | [prompt_doc_sync] docs/DEVELOPMENT.md, the new sign:runtime-manifest example: the line '--in       runtime-manifest.json           # the candidate's unsigned manifest' has NO trailing backslash (and an inline comment) inside a multi-line continuation command, so the documented owner signing command breaks after the --in line when pasted — the remaining --sha256/--private-key/--authority/--out flags are lost and the signer exits with usage. | backlog | docs/BACKLOG.md §Ф4+Ф5 |
| 3.1.0-Ф4Ф5 | confirm | triad-sol | X189 | AuthSheet ephemeral-browser copy promised prevention of vendor-side sibling sign-out (INV-067 forbids the guarantee) | fixed | dc8f0a88 (risk-reduction wording) |
| 3.1.0-Ф4Ф5 | confirm | triad-sol | X190 | release.yml + release-workflow-check still said/required "schema-v3" after the v4 contract bump | fixed | this commit (v4 wording both sides; check enforces v4) |
| 3.1.0-Ф4Ф5 | confirm | triad-gemini | X191 | claims: setup-login-inline and recoveryCommand call controlApiFetch without Authorization → default 401s | declined-with-evidence | controlApiFetch (packages/cli/src/live.ts:317) sets Authorization: Bearer on EVERY call — both claims refuted by the function's own body; gemini fabrication class (X175/X180) |
| 3.1.0-Ф4Ф5 | seal | owner-directed protocol | X215 | v3.1.0 RELEASE CANDIDATE SEALED at 82c69417 (reviewed candidate 1a6c02b2 + reviewer-requested ledger bookkeeping X192-X214 only): Ф4Ф5 wave (4 sub-waves) + confirmation + targeted re-checks; criticals X182-X187/X189-X190 fixed, X191 declined-with-evidence; coverage receipts ok:true every round; full battery NODE_CHAIN_EXIT=0/SWIFT_EXIT=0 (2274 node, Kit 206, App 290) at 1a6c02b2 | sealed | RC_SHA=82c69417; awaiting owner-live: offline signing, candidate→publish, login batch |
| 3.1.0-Ф4Ф5 | live+final | owner live test | X216 | D-17 disclosure race: awaiting_user flips at permit time, the code sidecar lands seconds later journal-less — SSE clients attached at the flip never learn the code exists (the card rendered codeless; owner live test) | fixed | 6fa96cb0 + refactor (this commit): watcher armed at EVERY awaiting_user flip (single owner — create AND reconcile-adopt paths per the round-6 sol critical); gated race test |
| 3.1.0-Ф4Ф5 | live+final | owner live test | X217 | ASWebAuthenticationSession completion inherited MainActor isolation — SIGTRAP right after a successful device-code sign-in | fixed | 1b819e93 (@Sendable-nonisolated completion) |
| 3.1.0-Ф4Ф5 | confirm-2 | triad-sol | X218 | README said "Current status: v3.0" + device-auth "code in the Terminal"; DESIGN_SYSTEM onboarding said every CLI launches in Terminal — D-17 shipped in-app | fixed | 73a10a80 |
| 3.1.0-Ф4Ф5 | confirm-2 | triad-sol | X219 | publish never bound the promoted manifest's buildSha to the tag's commit — a stale candidate_run_id could publish an earlier build | fixed | a0ef4f66 (--expected-build-sha from prepare.outputs.sha; workflow-check green) |
| 3.1.0-Ф4Ф5 | final | triad-sol | X220 | ledger ended at X215 while post-seal commits fixed further criticals (this bookkeeping gap) | fixed | rows X216-X220 (this commit) |
| 3.1.0-Ф4Ф5 | final-2 | triad-sol | X221 | restart adoption of an already-awaiting_user device-code job never re-armed the disclosure watcher — a sidecar landing under the successor daemon stayed invisible to SSE clients | fixed | watcher armed unconditionally in observeAndPermit with an idempotent per-job registry; successor-adoption race test (58/58) |
| 3.1.0-Ф4Ф5 | final-3 | triad-sol | X222 | X192-X214 ledger/backlog rows were mechanically truncated mid-word — not usable adjudicated findings | fixed | rows rebuilt from the sealed round-1 reports at sentence boundaries (this commit) |
| 3.1.0-Ф4Ф5 | attestation | triad-sol | X223 | the shipped D-2 closure could not be produced from the packaged app: build-app.sh deployed+signed playwright's optional fsevents.node inside browser-mcp-runtime while assertNoNativeAddons forbids ANY .node — the CI candidate died exactly there; local batteries never ran the closure builder against the packaged layout | fixed | fsevents pruned from the deployed runtime (chokidar polls without it), sign list corrected, and build-app.sh now runs build-runtime-closure against the freshly packaged app as a mandatory smoke — proven green locally |
| 3.1.0-Ф4Ф5 | seal-wave | triad-sol | X224 | boundedTail redacts AFTER the ring truncation — a secret split by the byte boundary could leave a prefix-less fragment redactSecrets cannot pattern-anchor, leaking into a failure outputTail | fixed | 5c3508f4→(this commit): the leading partial token is dropped before redaction, so a boundary-split fragment cannot survive |
| 3.1.0-Ф4Ф5 | seal-wave | triad-gemini | X225 | verify-signed-runtime-manifest --expected-build-sha skipped its check on an EMPTY string (falsy) — a prepare.outputs.sha that resolved empty would silently drop the tag-commit binding (X219 undercut) | fixed | flag presence now requires a valid 40-hex; empty/malformed fails loudly |
| 3.1.0-Ф4Ф5 | seal-wave | triad-gemini | X226 | claim: update-repo-metrics.mjs importing repo-asset-authority.ts crashes because the workflow runs plain node | declined-with-evidence | node 24.16.0 strips .ts types by default AND repo-metrics.yml pins node-version-file .node-version (24.16.0); empirically IMPORT-OK and the script ran (stars=256…). gemini fabrication class (X175/X180/X191) |
| 3.1.0-Ф4Ф5 | seal-wave | triad-fable | X227 | the X224 boundary fix never fired on the path it was accepted for: boundedTail gated on plain.length > cap, but the ring already trims to <=4096 bytes before decode, so a ring-boundary-split secret still leaked its prefix-less fragment | fixed | createTailBuffer tracks overflow and passes an explicit truncated flag to boundedTail; direct callers OR their own byte-budget check; exported helpers + a ring-boundary secret unit test (fragment dropped, trailing kept) |
| 3.1.0-Ф4Ф5 | seal-wave-2 | triad-fable | X227 | the X224 boundary drop never fired on the RING path — createTailBuffer truncates at the byte ring, then boundedTail sees a string that can never exceed the UTF-16 gate; a ring-boundary-split secret still reached outputTail | fixed | createTailBuffer reports overflow explicitly; boundedTail takes a truncated flag; ring-boundary secret unit test (setup-login-tail.test.ts) |
| 3.1.0-Ф4Ф5 | seal-wave-2 | triad-gemini | X228 | claudexord.ts main() ran at module evaluation — the probe test's import boots daemon init in a vitest worker, racing the LIVE daemon for the writer lease (impact latent; promised round-5 guard never landed) | fixed | entry guard: main() only when invoked as the process entry (pathToFileURL(argv[1]) match) |
| 3.1.0-Ф4Ф5 | seal-wave-2 | triad-gemini | X229 | runnerBootstrapEnv stripped proxy/CA env (HTTP(S)_PROXY, NO_PROXY, SSL_CERT_*, NODE_EXTRA_CA_CERTS) — behind a corporate proxy the detached device-code runner had no network path | fixed | pass-through-only allowlist extension |
| 3.1.0-Ф4Ф5 | seal-wave-2 | process | X230 | OWNER_REVIEW_MAX_ROUNDS as a tight bound forced dead cap-bump commits (10→12→…) while the real process law lives in CHECKLISTS/AUTHORIZATION | fixed | constant reframed as a runaway backstop (24) by reviewed commit |
| 3.1.0-Ф4Ф5 | seal-wave-3 | ALL FOUR sub-waves (fable+sol, 6 findings, 1 root) | X231 | the X228 entry guard silently killed the published claudexord npm bin — the wrapper IMPORTS the module, argv[1] is the wrapper path, main() never ran | fixed | runClaudexordEntry() exported; the wrapper calls it EXPLICITLY (import is side-effect-free, proven by an import probe); direct execution keeps the guard; probe tests green |
| 3.1.0-Ф4Ф5 | seal-wave-4 | triad-fable | X232 | the X231 wrapper rewrite was LOST (the fixing script died on an assert before writing the file) — the shipped wrapper still bare-imported and the npm daemon bin stayed dead; my import-probe "proof" proved exactly the bug | fixed | wrapper rewritten and proven BY EXECUTION: `node bin/claudexord.js --probe` returns {version, buildSha=candidate} |
| 3.1.0-Ф4Ф5 | seal-wave-4 | triad-fable | X233 | ARCHITECTURE said profiles login NEVER uses a daemon setup job — stale twice over since D-17 routed codex profile login through the durable device-code job | fixed | both spots tell the split truth (codex→setup job; others→interactive terminal) |
| 3.1.0-Ф4Ф5 | seal-wave-4 | triad-gemini | X234 | claims: setup-login-runner references pathToFileURL without import (no such code exists in that file — grep empty), and setup-login-inline lacks Authorization (third repeat of X191, refuted by controlApiFetch:317) | declined-with-evidence | fabrication class X175/X180/X191/X226 |
| 3.1.0-Ф4Ф5 | ci-candidate | signing CI | X235 | the fsevents prune (X223) left pnpm's symlinks DANGLING — signed-bundle codesign --verify dies on a broken link; the CI candidate failed exactly there while the local unsigned build never enters the signing branch | fixed | dangling-symlink sweep after the prune; proven locally (BUILD_EXIT=0, zero dangling links in the packaged bundle) |
| 3.1.0-Ф4Ф5 | seal-wave-5 | triad-gemini | X236 | claim: extractOutput's empty deliverable crashes a downstream JSON.parse("") fatally | declined-with-evidence | structuredOutput.ts:130 guards empty text with an honest reason and :133 wraps JSON.parse in try/catch; the green battery includes the X186 output-shape tests. Fabrication class; the pkg slot re-run on the SAME candidate returned 0 criticals |
| 3.1.0-post | post-release audit | codex-sol scouts (deep-scan, run truncated by quota) | X237 | D-15 intent gap AT THE TAG: the daily metrics cron shipped COMMENTED OUT in a3711d6b (release-freeze pause) and was only re-enabled in 7c1c5803 after publish — the tag itself does not carry the promised schedule | disclosed | intended and documented as the freeze runbook step, but the TAG is honestly a snapshot without the cron; the shipped behavior (dispatch-only) was never user-facing |
| 3.1.0-post | post-release audit | codex-sol scouts | X238 | QA-024 delegation belt is NOT hostable from the packaged macOS app at the tag — it fails typed rather than false-succeeding | backlog | honest failure, not a lie; docs/BACKLOG.md row for a follow-up (packaged-app delegation hosting) |
| 3.1.0-post | post-release audit | self | X239 | the final program audit could not complete: the claude profile hit 96% of its weekly quota (limit_action=fail) and the retry found no eligible harness after quota routing — the audit's four scouts DID produce the findings above before the reducer stage | disclosed | re-run the full audit after the quota window resets (2026-07-31); the release itself is unaffected (published, verified, smoke-tested) |
| 3.2.0-discovery | wave-1 | native critics, triad-fable | X240 | project `protected_paths` was evaluated only after a default live `in_place` thread had already mutated the project, contradicting the promised pre-apply human boundary | fixed | this commit: mutating protected-path threads promote durably to their persistent isolated worktree before adapter spawn; old-cwd sessions stale; direct live embedders fail before spawn; negative controls cover both paths |
| 3.2.0-discovery | wave-1 | triad-sol, triad-fable | X241 | both exact-candidate full-gate attempts passed Node/Swift/app/ZIP but failed when the Codex host denied hdiutil's temporary `/Volumes` mount; a green receipt is mandatory before attestation | fixed | exact final synthesis candidate passed the unchanged full gate in a Terminal host context, including DMG; receipt SHA-256 `2a25a8e8f216939ec978b115b31be727be7960ffce8681baaa6ee5fc039e12bc`, candidate unchanged |
| 3.2.0-discovery | wave-1 | triad-sol, triad-fable | X242 | machine-readable `help --json` labeled ACP stable while public docs and Terminal Auth correctly label the surface experimental | fixed | this commit: ACP registry stability is experimental, pinned by the machine-catalog test |
| 3.2.0-discovery | wave-1 | triad-sol | X243 | WHITEPAPER does not add rationale for the new capability-gated ACP Terminal Auth projection | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-sol | X244 | ARCHITECTURE lacks a detailed ACP auth dispatch/runtime ownership map | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-sol | X245 | INTEGRATIONS still describes Claude bridge exclusion as marker-only instead of created-this-run plus byte equality | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-sol, triad-fable | X246 | SECURITY overstates managed-secret Keychain support; the v2 managed store is file-only while vendor-native state is separate | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-fable | X247 | `acp serve auth login codex --json` accepts the global flag but the experimental terminal path ignores it on success | backlog | ACP clients never pass the flag; docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-fable | X248 | gen-version JSON output and checked-in Prettier layout can differ, creating regeneration-only churn | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-fable | X249 | the new manual MCP Registry publisher is documented in integrations but not yet enumerated in the sole release checklist | backlog | docs/BACKLOG.md discovery/distribution advisories; first publish remains post-npm and independently gated |
| 3.2.0-discovery | wave-1 | triad-fable | X250 | claim that CONTRIBUTING's `release:verify:node` reference is unproven because root package.json was absent from the changed-file packet | declined-with-evidence | root package.json defines the script with every removed explicit gate; both exact-candidate full-gate attempts completed the Node chain green before the unrelated hdiutil failure |
| 3.2.0-discovery | wave-1 | triad-fable | X251 | post-publish MCP verification may fail if the registry normalizes `$schema` before returning the server record | backlog | future/live-only reachability; docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | triad-fable | X252 | ACP usage text has two hand-maintained owners in auth dispatch and command registry | backlog | docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | native immune critic | X253 | ACP Codex auth description calls the default login a subscription profile although no profile id is supplied | backlog | experimental opt-in copy only; docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | wave-1 | native immune critic | X254 | experimental ACP Terminal Auth was absent from the mandatory non-solid feature ledger | fixed | this commit: concise `acp/auth` works-with-caveats row and count update |
| 3.2.0-discovery | synthesis | triad-sol, native distribution critic | X255 | manual MCP publication proved a stable annotated tag and current descriptor/package parity independently, but never required the input tag to equal `v${package.json.version}` | fixed | this commit: fail closed before OIDC unless the tag matches the root version; release-workflow-check carries a load-bearing binding assertion |
| 3.2.0-discovery | synthesis | triad-sol | X256 | enabling protected paths after earlier live in-place work may promote from repository state without explicitly transferring modified/untracked live bytes | backlog | opt-in transition, not default-reachable; docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | synthesis | triad-fable | X257 | gen-version JSON formatting can diverge from Prettier layout | duplicate-backlog | duplicate of X248; no new work item |
| 3.2.0-discovery | synthesis | triad-fable | X258 | SECURITY overstates managed-secret Keychain support | duplicate-backlog | duplicate of X246; no new work item |
| 3.2.0-discovery | synthesis | triad-fable | X259 | X241 still said open-gate after the exact final candidate obtained a green full-gate receipt | fixed | this commit updates X241 with the bound receipt digest |
| 3.2.0-discovery | synthesis | triad-fable | X260 | a legacy or hand-edited isolated no-project thread could reach worktree setup for the synthetic root | backlog | non-default legacy state; docs/BACKLOG.md discovery/distribution advisories |
| 3.2.0-discovery | synthesis | triad-fable | X261 | `threadWorktreeMutation` infers promotion from `in_place`, so a hypothetical future caller could change workspace identity unintentionally | backlog | future-caller risk only; docs/BACKLOG.md discovery/distribution advisories |
| 3.1.1-release | wave-1 | triad-sol (engine sub-wave) | X262 | B1: the review-loop prompt tells reviewers INSUFFICIENT_EVIDENCE blocks while demanding evidence for every blocking claim, and orchestrator isBlocking() lets an evidence-deficient reviewer result block convergence | declined-with-evidence | OWNER-DECLINED under BLOCKER_FILTER: the INSUFFICIENT_EVIDENCE-blocks verdict mapping is UNCHANGED since v3.1.0 (packages/review/src/reviewPrompt.ts:24 byte-identical at a3711d6b, the v3.1.0 tag); this diff only ADDED clauses restating the existing contract — a blocker on code unchanged since the released base re-litigates shipped behavior. The design question (should INSUFFICIENT_EVIDENCE be non-blocking?) is a backlog item, not a v3.1.1 blocker; re-raise only with NEW evidence of a defect introduced in v3.1.0..candidate |
| 3.1.1-release | wave-1 | triad (engine sub-wave) | X263 | B2: a cross-model effort CLAMP was not disclosed the way a DROP is (INV-105) — a moved level could ride silently | fixed | 91b9dc5d (#76): codex/claude clamp-disclosure seams on the same inputs the arg builders resolve with, pinned by tests |
| 3.1.1-release | confirm-wave | triad-sol (confirm sub-wave) | X264 | C1 (INV-105): probeClaudeEffortLevels trusted the 2.1.165 snapshot on ANY installed version when the help parse failed — an installed 2.1.89 was sent `--effort xhigh`, a level it rejects, and the fallback test pinned the faulty behavior as "usable" | fixed | this commit: version-gated snapshot trust (claudeSnapshotTrustedForVersion / claudeAdvertisedEffortsForRun / claudeRunEffortResolution) — a MISMATCHED-version fallback emits no effort flag and discloses through the existing drop seam, a same-version fallback keeps trusting the snapshot; the pinning test now states the honest semantics, and the mismatch adapter test was proven non-vacuous by reverting the gate |
| 3.1.1-release | confirm-wave | triad-sol (meta sub-wave) | X265 | C2: the v3.1.1 CHANGELOG entry omitted the #75 portable discovery/distribution surfaces, and release.yml extracts exactly this entry as the public GitHub Release notes | fixed | this commit: the entry now covers protected-path thread promotion, the portable Copilot plugin/Agent Skill, MCP Registry publication metadata, and experimental ACP Terminal Auth with their experimental/caveat status |
| 3.1.1-release | confirm-wave-r3 | triad-sol (confirm sub-wave) | X266 | C3 (INV-105): codexEffortCapability fell back to the 0.144.1 CODEX_EFFORT_SNAPSHOT on ANY installed version when the live model/list probe failed — CODEX_EFFORT_SNAPSHOT_VERIFIED_AGAINST was declared but never consulted, so runCodex emitted `model_reasoning_effort` from another version's catalog (the same class as C1/X264 on claude) | fixed | this commit: version-gated snapshot trust mirroring the claude seam (codexSnapshotTrustedForVersion / codexCatalogForRun / codexRunEffortResolution) — a MISMATCHED-version fallback yields an EMPTY catalog, sends no effort flag, and discloses through the existing drop seam; a same-version fallback keeps trusting the snapshot and a LIVE probe is trusted on any version; adapter tests cover mismatch/same-version/live and the profile CODEX_HOME path, the mismatch test proven non-vacuous by reverting the gate |
| 3.1.1-release | confirm-wave-r3 | triad-sol (portable-b sub-wave) | X267 | C4: SECURITY.md said update lookups are "explicitly user-invoked", contradicting README and docs/ARCHITECTURE.md, which document the app's engine update check firing on app FOREGROUND as well — an understated network-behavior claim in the security policy | fixed | this commit: SECURITY.md now states foreground-or-explicit for the app's update check while keeping the no-background-timer guarantee, matching README and ARCHITECTURE wording |
| 3.1.2-release | synthesis | owner-directed Delegate recovery | X268 | X238 was true at the v3.1.0 tag but its stale backlog pointer made the packaged-app Delegate hosting defect read as still open | fixed | this release: the exact packaged daemon self-entry hosts `mcp serve-belt`; npm/app closure smokes, required-MCP adapter tests, durable lineage/budget/cancel contracts, and macOS reload projections gate publication |
| 3.1.2-release | synthesis | contract red-team | X269 | a mixed capable/incapable Delegate pool projected the run as effective without disclosing that one selected lane continued as ordinary Agent | fixed | typed `partially_degraded` receipt and remediation across telemetry, Control API, CLI, and macOS; an Orchestrator integration test runs both lanes and proves the persisted per-lane receipts |
| 3.1.2-release | synthesis | contract red-team | X270 | an empty reviewer panel still acquired a fictitious review lease, so ordinary no-reviewer Delegate work could consume authority it never used | fixed | review leases are created only when reviewers exist; shared-family happy-path and terminal tests pin the zero-reviewer case |
| 3.1.2-release | synthesis | contract red-team | X271 | child cash could settle during the Delegate drain after the parent had written a successful decision and terminal, allowing a late overshoot or unverifiable cost to escape the parent result | fixed | the announced-run barrier reconciles settled cash, valuation, and estimation into the decision and replaces only a prepared success with the typed budget failure; cancellation and existing failures retain their original facts |
| 3.1.2-release | synthesis | contract red-team | X272 | child-drain timeout/error facts and ordinary non-Delegate terminal paths could diverge or terminalize twice under the new post-drain recheck | fixed | drain failures update the decision before Control projection, and the recheck is armed only while the exact run owns active Delegate family authority; terminal-count negative controls cover ordinary runs |
| 3.1.2-release | synthesis | UI concurrency review | X273 | late run-detail, artifact, and thread-reconciliation responses could repaint a newly selected thread, clear a newer same-run request, or leave a failed direct A→B reconciliation falsely clean | fixed | client/generation/thread/load-token identity fences, retryable failed loads, and a dirty-until-success reconciliation state; Swift concurrency regressions cover reconnect and direct thread switches |
| 3.1.2-release | synthesis | UI truth review | X274 | restored child rows could follow global run-list order instead of canonical turn lineage, a failed projected Changes load rendered as `No changes`, and the new typed drain failure code was absent from Swift diagnostics | fixed | projected child IDs own chronology, missing projected detail is a retryable failure, and `delegation_child_drain_timeout` crosses generated wire fixtures into the failure headline |
| 3.1.2-release | synthesis | adapter contract review | X275 | Delegate `used` evidence recognized only Claude's namespaced tool name; Codex records the native tool name plus `server:tool` target, while a prefix-like non-MCP tool also had to be rejected | fixed | one adapter-neutral exact matcher requires `kind=mcp` and bounded server delimiters for both normalized shapes; Claude/Codex parser and collision controls are green |
| 3.1.2-release | synthesis | packaging review | X276 | the packaged belt smoke proved module initialization but not that the exact bundled daemon entry could host and answer the six-tool belt over a real scratch socket; long temporary paths also risked macOS socket failure | fixed | two-phase smoke asserts the six-tool allowlist, boots the exact packaged entry under canonical `/tmp`, executes a daemon-origin missing-run round trip, and stops only its recorded PID |
| 3.1.2-release | synthesis | projection review | X277 | decision-less runs emitted `budget.cash.estimated`, but Control API ignored that flag and presented estimated metered cash as exact | fixed | the last authoritative cash receipt now carries its estimation truth into `ControlBudgetSnapshot`; an integration regression removes the decision and proves the projected flag |
| 3.1.2-release | immune scan | whole-tree docs truth | X278 | the macOS README still described the retired per-run Run Detail plus separate Workbench and Canvas planes, contradicting INV-091 and the shipped current-thread Changes/Artifacts/Evidence workspace | fixed | current behavior is described as persistent inline receipts plus one thread workspace; produced outputs and previews fold into Artifacts and selecting a receipt only filters the workspace |
| 3.1.2-release | immune scan | whole-tree capability truth | X279 | the newly merged site claimed every harness, including OpenCode, could unconditionally plan, implement, review, or continue although Doctor disables roles whose required smoke is unavailable | fixed | the site now says participation is conditional on Doctor enabling the requested role and that unavailable roles stay disabled with their reason |
| 3.1.2-release | immune scan | whole-tree UI discipline | X280 | a Delegate child title shared a free-flowing HStack with its badge and phase, so text length could move the trailing facts in violation of INV-134 anchored clusters | fixed | the title owns the flexible cell, phase owns a fixed trailing cell, and a structural Swift regression forbids the internal Spacer that caused drift |
| 3.1.2-release | immune scan | inherited site accessibility | X281 | the merged landing page still scheduled an infinite canvas frame loop and rotating aside text when `prefers-reduced-motion` was active | fixed | reduced-motion draws one static canvas frame and does not start the rotating-text interval |
| 3.1.2-release | synthesis confirmation | docs truth | X282 | the INV-091 README repair incorrectly said internal run-tree artifacts and produced outputs shared the Artifacts gallery; the app keeps internal artifacts with diagnostics under Evidence | fixed | the README now assigns produced outputs and preview to Artifacts and internal run-tree evidence to Evidence |
| 3.1.2-release | synthesis confirmation | negative-control review | X283 | the new INV-134 structural test searched the whole row for a flexible frame, so identical outer frames let it stay green if the title anchor regressed | fixed | the assertion is bounded to the exact source slice between `Text(child.title)` and `Text(receipt.badge)`; removing the title frame now fails |
| 3.1.2-release | live Delegate acceptance | Claude Code 2.1.165 | X284 | Claude reports an asynchronously starting inline MCP server as `pending`, but the required-server parser rewrote every non-ready status to `failed` and killed the healthy belt before it connected | fixed | ready and provisional statuses are distinct; `pending`/`connecting`/`starting` continue without claiming readiness, while missing, malformed, unknown, and explicit terminal statuses remain fail-closed; a subprocess regression proves pending is not killed |
| 3.1.2-release | synthesis red-team | required-belt outcome | X285 | an exact belt tool error could be reduced to an ordinary warning when the parent still produced a deliverable, allowing a native fallback to hide the failed injected operation | fixed | INV-030 now defines the required-capability exception; an unrecovered exact tool+kind+target failure hard-fails race and convergence before review, while a matching success recovers under the existing INV-043 key and the Delegate receipt remains the honest `used` path fact |
| 3.1.2-release | synthesis red-team | terminal causality | X286 | race review-policy output and convergence cancellation could overwrite the real Delegate tool-failure cause depending on finalization timing | fixed | cancellation/deadline keeps highest precedence; otherwise the belt failure terminalizes with `delegation_runtime`, `harness_error`, harness/attempt provenance, and the raw attempt reference; mixed-race, convergence, recovery, and cancellation controls are green |
| 3.1.2-release | live Delegate acceptance | Claude Code child launch | X287 | after the belt connected and the exact tool ran, the child defaulted its project to the parent harness envelope, which the daemon correctly rejected as a Claudexor-owned root | fixed | the engine now binds the normalized original user-project root into the belt policy; descriptor, orchestrator, bridge, runner, and refusal negatives prove an empty root fails closed and raw tool paths cannot redirect the child |
| 3.1.2-release | synthesis red-team | failed in-place receipt | X288 | the new early Delegate failure terminal preserved no WorkProduct for live bytes already written by an explicitly in-place lane, leaving no durable applied state or revert fence | fixed | race and convergence persist `adopted:true` plus `applied_review_blocked`, pre/post snapshots and a revert anchor, emit `work_product.emitted` before `run.failed`, and prove the anchor restores the live tree |
| 3.1.2-release | synthesis test review | create receipt parity | X289 | the early failed in-place helper always labeled its WorkProduct `patch`, so reachable `create --in-place --delegate` failures mis-typed already-written new-project bytes | fixed | race and convergence pass the intent-derived WorkProduct kind through the shared helper; the create negative proves `new_repo` while retaining applied-review-blocked, event-order, and revert guarantees |
| 3.1.2-release | full-gate synthesis | readability ratchet | X290 | the first exact-candidate gate passed all 2,588 Node tests but rejected new Delegate terminal logic left inline in the two ratcheted orchestration owners | fixed | shared failure classification, terminal provenance, and failed in-place persistence now live in one focused `delegationFailure` module; the orchestrator and attempt telemetry are at or below their locked baselines without raising the ratchet |
| 3.1.2-release | packet preflight (rejected) | triad-sol, packet contract audit | X291 | the first external review packet omitted `DECISION_REGISTRY.md`, `DECLINED_FINDINGS.md`, and `BLOCKER_FILTER.md`; the frozen-packet validator accepted it despite the sole CHECKLISTS protocol requiring all three | fixed | `FROZEN_REVIEW_EVIDENCE_FILES` now requires the three distinct authorities and a negative-control test proves that a fully re-sealed manifest missing any one fails before review; the incomplete packet and its stopped outputs are invalid and will not be sealed |
| 3.1.2-release | packet preflight (rejected) | triad-sol, terminal adversarial audit | X292 | a late Delegate-family budget failure replaced the decision, summary, result, and terminal event but left an already-written successful `final/work_product.yaml` stale and announced no diagnostic readiness for the replacement summary | fixed | the shared terminal guard reconciles WorkProduct lifecycle/outcome facts before terminalization, preserves review/apply/revert/extensions, and emits the replacement diagnostic `output.ready`; late-overshoot and child-drain-timeout negatives cover both paths |
| 3.1.2-release | packet preflight (rejected) | triad-sol, best-of adversarial audit | X293 | claim: `claudexor_best_of` omits required `n`, so the belt can silently run a single ordinary Agent | declined | the named belt handler injects `race:true`, and `mcpSurfaceRunner` deterministically maps an absent `n` to `n:2` before internal enqueue; a direct body-mapping regression now pins the existing behavior |
| 3.1.2-release | synthesis adversarial audit | terminal causality | X294 | a real child-drain timeout inherited the enclosing `race` or `convergence` phase even though the failure arose in the Delegate terminal barrier; the old test hid the mismatch by manually announcing `delegation_drain` | fixed | the guard records barrier origin and stamps `delegation_drain` in both `failure.yaml` and `run.failed`; the test now begins with the production `race` phase and asserts both projections |
| 3.1.2-release | synthesis adversarial audit | terminal facts | X295 | the first WorkProduct reconciliation draft rebuilt failure facts from defaults, which would discard independent passed-checks, approved-review, no-change, or work-state evidence | fixed | lifecycle/reason now overlay the prior canonical facts while review/check/apply facts and extension receipts remain unchanged; focused tests pin preservation through overshoot and drain timeout |
| 3.1.2-release | synthesis adversarial audit | Delegate drain liveness | X296 | an injected asynchronous `cancelAdmission` that never settles can delay entry into the bounded child-drain timeout | backlog | not default-reachable because the shipped daemon binding is synchronous; recorded in `docs/BACKLOG.md` for any future asynchronous embedder |
| 3.1.2-release | synthesis adversarial audit | terminal result parity | X297 | after a prepared race/convergence result, a child-drain timeout reconciled artifacts but returned the generic failure shell with `winner:null` and empty candidates, disagreeing with the durable product provenance | fixed | the failure terminal overlays lifecycle/facts/summary/spend onto the prepared result, preserving winner, candidates, review verification, and other lineage; the timeout negative pins the returned parity |
| 3.1.2-release | synthesis adversarial audit | cancellation precedence | X298 | cancellation arriving during the bounded Delegate child drain could lose to the already-prepared success, leaving succeeded events/artifacts while the daemon independently stored the job as cancelled | fixed | an armed Delegate barrier rechecks abort before budget or success flush, clears the deferred success, reconciles decision and WorkProduct to cancelled while preserving independent/product facts, and emits one cancelled terminal; a drain-time abort negative covers the race |
| 3.1.2-release | exact-candidate adversarial review | cancel-timeout interference | X299 | if cancellation arrived during Delegate drain and that same barrier rejected with its timeout, the catch cancellation path reset winner/candidates/checks/review and left WorkProduct at the prepared success | fixed | both resolved and rejected barrier cancellation paths now reconcile from the prepared result and preserve its provenance/facts; the parameterized negative aborts then throws `delegation_child_drain_timeout` |
| 3.1.2-release | exact-candidate adversarial review | cancelled failure artifact | X300 | when cancellation overrode a prepared Delegate failure during drain, the old `final/failure.yaml` survived even though result/events/decision/WorkProduct became cancelled and the Control schema permits failure detail only for failed lifecycle | fixed | cancellation removes the superseded failure artifact before its terminal; the prepared-failure negative proves preserved provenance, cancelled facts, the two honest summary readiness updates, one terminal, and no failure file |
| 3.1.2-release | synthesis gate | readability ratchet | X301 | the combined terminal-interference repair pushed `runTerminals.ts` four lines over the 600-line non-legacy cap | fixed | WorkProduct terminal reconciliation and independent-axis replacement moved to one focused `terminalOutcome.ts` owner; complexity ratchet passes without a baseline raise |
| 3.1.2-release | frozen wave-1 control-core | triad-sol | X302 | run-producing belt policy refusals and failed/cancelled/interrupted children crossed MCP as successful tool results | fixed | 5ba912b4: safe typed errors now cross the real MCP wire as `isError:true` after spend reconciliation; successful runs and read-only result inspection remain readable |
| 3.1.2-release | frozen wave-1 control-core | triad-sol | X303 | parent run detail uses uncached, non-degrading child projection and can 500 on a corrupt child record | fixed | 0fbb8a4c, d4338b8a: parent detail now uses the cached fail-soft live projector; corrupt children remain diagnostic without failing the parent |
| 3.1.2-release | frozen wave-1 control-core | triad-fable | X304 | duplicate child-detail cache/fail-soft projection finding | fixed | 0fbb8a4c, d4338b8a: fixed with X303 |
| 3.1.2-release | frozen wave-1 control-core | triad-fable | X305 | `paramsRecord` has two private copies beside the new shared owner | backlog | ownership cleanup only; tracked in the 3.1.2 external-review advisories section of `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 control-core | triad-fable | X306 | a signalled-but-undrained Delegate-family cancellation returns 503 and can invite a confusing retry | backlog | signalling remains audited and cancellation continues asynchronously; response-shape refinement is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-sol | X307 | the current-main site redesign was not named in the Delegate decision registry | declined | D312-11 records the owner's approval to include the already-merged site release content under the same candidate gates |
| 3.1.2-release | frozen wave-1 docs-rest | triad-sol | X308 | frozen-packet mandatory-file validator does not separately require `DIFF_SHA256.txt` | backlog | this candidate packet contains and binds the digest; validator hardening is non-default process work tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-sol | X309 | failed in-place create receipts can disagree between WorkProduct `kind` and `meta.result_kind` | backlog | explicit `create --in-place --delegate` advisory; tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-sol | X310 | `packages/util/CHANGELOG.md` has no 3.1.2 note for the exported delegation project-root field | backlog | package changelog completeness advisory tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-sol | X311 | secrets package metadata still claims an OS-Keychain managed-store branch | backlog | duplicate of X246; no new work item |
| 3.1.2-release | frozen wave-1 docs-rest | triad-fable | X312 | Codex required-MCP fatal parsing and connected inference lack a recorded native fixture | backlog | live 0.144.1 acceptance proves the current path; fixture/version provenance is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-fable | X313 | the Swift drain-timeout fixture says `terminalization` while production emits `delegation_drain` | backlog | typed decode is unaffected; fixture fidelity correction is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 docs-rest | triad-fable | X314 | the packet manifest did not contain a standalone targeted secret-scan receipt | backlog | the exact diff was scanned successfully before transport and packet verification scans every payload; sealing the targeted receipt is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 engine-adapters | triad-sol | X315 | a successful later call of the same belt tool could recover an earlier different invocation because adapter targets are not operation-specific | fixed | 5ba912b4: recovery keys by matching non-null native tool-use identity when available and retains tool+kind+target fallback only for legacy/no-id telemetry |
| 3.1.2-release | frozen wave-1 engine-adapters | triad-sol | X316 | subscription valuation uncertainty can mark proven zero cash as estimated | fixed | 5d9c084f, 83f42d92: cash certainty and valuation knowledge are independently owned from per-route usage through ledger, Control, and macOS projections |
| 3.1.2-release | frozen wave-1 engine-adapters | triad-fable | X317 | deferred EventLog terminal returns a provisional object with a sequence later used by another persisted event | backlog | no current consumer uses the return; API-hardening work is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 engine-adapters | triad-fable | X318 | duplicate signalled-but-undrained cancellation response finding | backlog | duplicate of X306; no new work item |
| 3.1.2-release | frozen wave-1 engine-adapters | triad-fable | X319 | success-path decision budget reconciliation is unguarded and broader than the Delegate barrier | fixed | 5d9c084f, 51c23928, 83f42d92: success reconciliation is Delegate-drain-owned; guard-created ordinary failure and cancellation reconcile only terminal facts |
| 3.1.2-release | frozen wave-1 orchestrator | triad-sol | X320 | secret-bearing in-place output could throw before preserving real Delegate telemetry, leave live bytes, and make the engine create an additional dangling Git blob | fixed | 5ba912b4: raw diff stays memory-only in a private object directory, checked rollback precedes artifact writes, and only a sanitized truthful receipt persists; harness-written Git state is separately reported as manual cleanup under D312-10 |
| 3.1.2-release | frozen wave-1 orchestrator | triad-sol | X321 | claim: race attaches the child budget authority before `run.created`, so an event failure leaks the attachment | declined | exact candidate orders `run.created` before `rootLedger` in race, plan, and report; the real opposite ordering gap is independently ledgered as X339 |
| 3.1.2-release | frozen wave-1 orchestrator | triad-fable | X322 | duplicate unguarded success-path decision reconciliation finding | fixed | 5d9c084f, 51c23928, 83f42d92: fixed with X319 |
| 3.1.2-release | frozen wave-1 orchestrator | triad-fable | X323 | cancellation can lose to a drain-barrier rejection when the strategy body also throws | backlog | narrow non-default three-way race; tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 orchestrator | triad-fable | X324 | duplicate deferred EventLog provisional-sequence finding | backlog | duplicate of X317; no new work item |
| 3.1.2-release | frozen wave-1 orchestrator | triad-fable | X325 | mixed startup-failed plus degraded lanes suppress the aggregate degraded remediation nuance | backlog | terminal cause remains correct and per-lane receipts retain truth; copy/projection refinement is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 schema-ui | triad-sol | X326 | macOS artifact preview bounds Unicode characters rather than exact UTF-8 bytes | fixed | 0fbb8a4c: preview truncation now measures the encoded UTF-8 byte budget |
| 3.1.2-release | frozen wave-1 schema-ui | triad-sol | X327 | a row removed during detail loading can still be marked hydrated and later suppress a needed reload | fixed | 0fbb8a4c: hydration is recorded only after the target row is actually updated |
| 3.1.2-release | frozen wave-1 schema-ui | triad-fable | X328 | duplicate deferred EventLog provisional-sequence finding | backlog | duplicate of X317; no new work item |
| 3.1.2-release | frozen wave-1 schema-ui | triad-fable | X329 | `clearDeferredTerminal` leaves deferral armed and exposes a future misuse path | backlog | sole current consumer re-emits and flushes on every path; API-hardening work is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 schema-ui | triad-fable | X330 | an eager thread-detail request is not cancelled when identity fences reject its result | backlog | stale writes are fenced; resource cleanup is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | frozen wave-1 schema-ui | triad-fable | X331 | composer full-access check omits the wire-only `external_sandbox_full` profile accepted by the engine | fixed | 0fbb8a4c: composer and engine now classify `external_sandbox_full` identically |
| 3.1.2-release | frozen wave-1 schema-ui | triad-gemini | X332 | macOS keeps a hand-written degraded explanation beside the server remediation fact | backlog | UI already consumes typed reason/facts; vocabulary ownership cleanup is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | Fable-high whole-diff check | control-core | X333 | child-detail fail-soft/cache finding repeated under explicit high reasoning | fixed | 0fbb8a4c, d4338b8a: fixed with X303 |
| 3.1.2-release | Fable-high whole-diff check | control-core | X334 | cancellation response finding repeated under explicit high reasoning | backlog | duplicate of X306; no new work item |
| 3.1.2-release | Fable-high whole-diff check | docs-rest | X335 | drain fixture phase mismatch repeated under explicit high reasoning | backlog | duplicate of X313; no new work item |
| 3.1.2-release | Fable-high whole-diff check | docs-rest | X336 | secrets package description finding repeated under explicit high reasoning | backlog | duplicate of X246/X311; no new work item |
| 3.1.2-release | Fable-high whole-diff check | engine-adapters | X337 | Codex required-MCP fixture gap repeated under explicit high reasoning | backlog | duplicate of X312; no new work item |
| 3.1.2-release | Fable-high whole-diff check | engine-adapters | X338 | deferred EventLog provisional-sequence finding repeated under explicit high reasoning | backlog | duplicate of X317; no new work item |
| 3.1.2-release | Fable-high whole-diff check | orchestrator | X339 | race, plan, and read-only report emit `run.created` before delegated root-ledger attachment can fail, leaving an announced nonterminal event | backlog | daemon job still terminalizes; narrow parent-close/child-start race tracked in `docs/BACKLOG.md` |
| 3.1.2-release | Fable-high whole-diff check | orchestrator | X340 | cancellation-versus-barrier rejection finding repeated under explicit high reasoning | backlog | duplicate of X323; no new work item |
| 3.1.2-release | Fable-high whole-diff check | orchestrator | X341 | unconditional decision-budget reconciliation can alter ordinary-run estimate semantics | fixed | 5d9c084f, 51c23928, 83f42d92: fixed with X319 |
| 3.1.2-release | Fable-high whole-diff check | schema-ui | X342 | deferred EventLog provisional-sequence finding repeated under explicit high reasoning | backlog | duplicate of X317; no new work item |
| 3.1.2-release | Fable-high whole-diff check | schema-ui | X343 | queued job-id detail restoration matches children against the alias instead of the real parent run id | fixed | d4338b8a: restoration resolves the real parent run id before matching server-owned child lineage |
| 3.1.2-release | synthesis scope review | complexity | X344 | the first combined blocker batch exceeded the locked line ratchet in orchestrator, workspace capture, and workspace manager owners | fixed | 5ba912b4: secret/revert/plain-diff responsibilities were extracted or consolidated; every owner is below its prior cap and no baseline was raised |
| 3.1.2-release | synthesis adversarial review | binary secret capture | X345 | binary secret detection reread mutable worktree bytes after creating the candidate tree, allowing a TOCTOU replacement to evade the fence | fixed | 5ba912b4: binary preimages and postimages are read from the exact base and immutable scratch tree before its private object directory is removed |
| 3.1.2-release | synthesis scope review | transient Git environment | X346 | an unquoted `GIT_ALTERNATE_OBJECT_DIRECTORIES` path breaks on a legal macOS repository path containing `:` | fixed | 5ba912b4: the alternate is C-style quoted and a colon-path regression captures successfully |
| 3.1.2-release | synthesis scope review | artifact exclusion | X347 | an early transient-capture draft omitted the existing `.claudexor-artifacts` exclusion, so owned screenshot output entered the candidate diff and made the F4 regression test fail | fixed | 5ba912b4: capture restores the exact top-level `.claudexor-artifacts` exclusion plus positively proven bridge paths; similarly named user paths remain captured |
| 3.1.2-release | synthesis gate | test typing | X348 | a new telemetry regression used an invalid empty object and failed strict test typechecking | fixed | 5ba912b4: the test now constructs telemetry through the production initializer and `typecheck:tests` passes |
| 3.1.2-release | synthesis adversarial review | thread delivery | X349 | thread apply retained a second capture path without immutable binary-secret parity | fixed | 5ba912b4: thread delivery consumes the common transient capture result and text/binary negative controls prove it writes no engine-owned candidate blob |
| 3.1.2-release | synthesis adversarial review | in-place truth | X350 | a successful worktree reverse could be reported as fully reverted even though a harness may already have written secret state into Git index, refs, or objects | fixed | 5ba912b4: every Git-backed in-place refusal remains `adopted:true`, `applied_review_blocked`, and `manual_cleanup`; no patch or anchor is persisted |
| 3.1.2-release | synthesis scope review | terminal remediation | X351 | a simultaneous Delegate belt failure and secret refusal could discard the secret cleanup action when building the dominant terminal | fixed | 5ba912b4: Delegate terminal construction merges the typed secret-refusal remediation with the belt failure actions |
| 3.1.2-release | synthesis scope review | failure accumulation | X352 | short-circuit assignment could skip recording a secret refusal when another harness error was already true | fixed | 5ba912b4: the refusal helper always records the error and then returns the combined boolean state |
| 3.1.2-release | synthesis scope review | non-Git rollback | X353 | plain `diff -N` missing-side headers reverse-applied a new file to an empty file while the receipt claimed exact restoration | fixed | 5ba912b4: structural missing-side headers become `/dev/null`; the non-Git negative now proves the added file is absent before recording `reverted` |
| 3.1.2-release | synthesis adversarial review | candidate Git authority | X354 | a harness `git add` or commit inside a disposable linked worktree wrote secret blobs and refs into the user's repository; a shared clone initially retained a writable local `origin` | fixed | 5ba912b4: disposable candidates use clone-local refs/index/objects, remove `origin` before harness start, and tests prove both no remote and no secret blob in the source object store |
| 3.1.2-release | synthesis adversarial review | raster outputs | X355 | secret-bearing raster bytes could bypass the text diff through the owned artifact directory or a gitignored markdown image link | fixed | 5ba912b4, 45532377, 5638cba5: both discovery lanes share the bounded component-fenced secret scan and persist no patch, answer, or produced media on refusal |
| 3.1.2-release | synthesis adversarial review | raster copy race | X356 | a preflight-only raster scan could copy different bytes after a concurrent replacement | fixed | 5ba912b4: each copy scans the exact bounded Buffer that is written through the no-follow destination path |
| 3.1.2-release | synthesis adversarial review | capture ownership | X357 | the unused historical `diffStaged` implementation remained a second unsafe scratch-index owner | fixed | 5ba912b4: all consumers use the single transient capture owner and the obsolete export was retired |
| 3.1.2-release | synthesis scope review | schema truth | X358 | envelope schema/comments still described source-repository worktree branches after candidates moved to clone-local authority | fixed | 5ba912b4: source and generated schemas now say private Git checkout and clone-local branch; the source repository never receives that ref |
| 3.1.2-release | synthesis adversarial review | explicit in-place boundary | X359 | an external harness can itself stage a secret before the engine's fence, leaving Git object residue that post-hoc inspection cannot disprove | declined | D312-10 and Bible INV-030 explicitly accept this boundary and require an honest manual-cleanup receipt rather than a false revert claim; private Git authority covers default isolated candidates |
| 3.1.2-release | synthesis adversarial review | shared clone lifetime | X360 | a disposable private clone reads source objects through an alternate and could lose an unreachable dirty-snapshot base during concurrent aggressive source GC | backlog | ordinary candidate lifetime keeps this non-default; self-contained snapshot pinning is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | synthesis adversarial review | refusal copy | X361 | the refusal message always said `patch diff` even when only a raster output triggered the fence | fixed | 5ba912b4: the shared diagnostic now accurately says candidate output and artifact persistence for every text, binary, and media source |
| 3.1.2-release | synthesis adversarial review | binary capture bounds | X362 | shell helpers with default output caps could truncate large binary preimage/postimage scans | fixed | 5ba912b4: binary object reads use the repository's streaming capture helper rather than a default-buffer `execFile` path |
| 3.1.2-release | exact-SHA confirmation | raster bounds | X363 | raster persistence bounded a stale pre-read stat, so a concurrent replacement could exceed the per-file and aggregate byte ceilings even though secret scan and write used the same Buffer | fixed | b70ddf28: source files are opened no-follow, fstat/read share one descriptor, actual Buffer length is checked against remaining capacity, and accounting uses bytes actually written |
| 3.1.2-release | exact-SHA confirmation | non-Git binary secret fence | X364 | non-Git in-place binary output produced only a payload-free `Binary files differ` stub and always reported `binarySecretLike:false`, allowing secret bytes to survive without a refusal | fixed | b70ddf28: changed live binary postimages are scanned through a bounded no-follow descriptor; unreadable or oversized bytes fail closed and a production negative proves no artifact persistence plus truthful manual cleanup |
| 3.1.2-release | targeted X364 confirmation | non-Git capture failure | X365 | unreadable binary output made `diff -ruN` exit 2 with empty stdout, but the manager ignored the exit code and treated the failed capture as a safe no-change result | fixed | ca54dd66: only diff exit 0/1 is accepted; missing baselines, exit >1, and capture/scan exceptions return an uncaptured binary risk that produces a sanitized manual-cleanup refusal, with manager and production chmod-000 negatives |
| 3.1.2-release | final Fable-high full-diff | control-core | X366 | claim: `FILES_TO_READ_WHOLE.txt` must repeat every changed CLI/MCP file or the review is under-covered | declined | the list is supplemental unchanged-file context; Git's NUL-safe changed-set enumeration is authoritative, all named files appear in the full diff, and their complete text is present in the corresponding engine/control packs |
| 3.1.2-release | final Fable-high full-diff | control-core | X367 | Delegate child summaries embedded in parent detail skip the live `waitingOnUser` interaction overlay | fixed | 0fbb8a4c, d4338b8a: the shared cached child projector applies the journal-backed waiting-on-user overlay |
| 3.1.2-release | final Fable-high full-diff | control-core | X368 | an unscoped belt status/result policy refusal returns plain success text rather than an MCP `isError:true` result | backlog | normal engine injection always supplies the server-owned parent id and still refuses scope safely; typed read-refusal parity is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final Fable-high full-diff | docs-rest | X369 | claim: omission of generated `WorkspaceEnvelope.schema.json` from `FILES_TO_READ_WHOLE.txt` proves packet under-coverage | declined | duplicate of X366: generated schemas are intentionally diff-authoritative, and the file is present in the Git-derived changed set and sealed full diff |
| 3.1.2-release | final Fable-high full-diff | docs-rest | X370 | the manually assembled final review packet made `DECISION_REGISTRY.md` byte-identical to `PLAN_ACCEPTED.md` rather than emitting a concise change-to-decision/invariant crosswalk | backlog | all D312-1..11 decisions remained available and scope adjudication was correct; packet-builder process hardening is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final Fable-high full-diff | docs-rest | X371 | final Fable-high repeats the Swift drain fixture `terminalization` versus production `delegation_drain` phase drift | backlog | duplicate of X313/X335; no new work item |
| 3.1.2-release | final Fable-high full-diff | docs-rest | X372 | final Fable-high repeats the stale OS-Keychain managed-store claim in the secrets package description | backlog | duplicate of X246/X311/X336; no new work item |
| 3.1.2-release | final Fable-high full-diff | engine-adapters | X373 | `docs/INTEGRATIONS.md` calls Delegate bootstrap values non-ledger metadata although they seed a conservative process-local refusal ledger beneath the daemon authority | fixed | a904ebea: the environment reference now names the conservative process-local refusal ledger while retaining daemon family accounting as authoritative |
| 3.1.2-release | final Fable-high full-diff | engine-adapters | X374 | simultaneous transient capture/revert failure and scratch-directory cleanup failure can replace the primary typed diagnostic with the cleanup error | fixed | 11ab618d, cbffbb64: capture and revert preserve the primary typed failure and attach the cleanup failure separately; cleanup-only failure remains terminal |
| 3.1.2-release | final Fable-high full-diff | orchestrator | X375 | a non-Git in-place text rollback can say `reverted` when the refusal was additionally triggered by a pre-existing Markdown-linked secret raster outside the diff | fixed | 11ab618d, d4338b8a: media risk retains path provenance and any risky raster outside the exact reversible patch forces a truthful manual-cleanup receipt |
| 3.1.2-release | final Fable-high full-diff | orchestrator | X376 | a raster already above the 16 MiB persistence ceiling is skipped rather than treated as an unprovable secret for an explicit in-place refusal | fixed | 11ab618d, d4338b8a: oversized and unreadable in-place rasters fail closed with a sanitized manual-cleanup receipt whose recovery detail names whether exact reversal failed or media remained outside the reversible patch |
| 3.1.2-release | final Fable-high full-diff | schema-ui | X377 | `RunDelegationInfo` couples schema validity to byte-identical user-facing remediation copy | backlog | every current producer uses the canonical helper and no default 3.1.2 mismatch exists; future mixed-version normalization is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-sol | X378 | confirmation packet `BLOCKER_FILTER.md` incorrectly called itself wave 1 and granted full-wave review authority | fixed | packet-only defect; the exact-final confirmation packet is rebuilt from its confirmation FREEZE/PHASE metadata and re-sealed before any slot is accepted |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-sol | X379 | recent fixed ledger rows named a batch but omitted its exact commit id | fixed | this ledger now cites 5ba912b4 for X302/X315/X320/X344-X362, b70ddf28 for X363-X364, ca54dd66 for X365, and 45532377/5638cba5 for the final media repairs |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-sol | X380 | qualified ledger dispositions were used without documenting their canonical meaning | fixed | the disposition contract above now maps every qualified form to declined, backlog, or features |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-sol | X381 | the 3.1.2 backlog preamble named only X303-X343 although later advisory rows were present | fixed | the preamble now lists every represented ledger range through this confirmation |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-sol, triad-fable | X382 | public rollback prose and the non-Git architecture fallback overstate exact reversal when capture is unavailable or binary bytes remain live | fixed | a904ebea: architecture now documents fail-closed sanitized manual cleanup when exact capture or reversal cannot be proven |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema, orchestrator / triad-fable | X383 | one secret-refused isolated candidate now dominates an otherwise healthy best-of race | fixed | bcb011b6: owner-locked D312-13 excludes a disposed isolated refusal when a safe working candidate survives; all-refused, in-place, and belt failures remain hard |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-fable | X384 | raster detection and persistence read separate snapshots, so a concurrent replacement can change refusal truth while still being skipped at copy time | backlog | stable symlink/root escapes are fixed in 5638cba5 and no unsafe bytes persist; descriptor-chain/concurrency hardening is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema / triad-fable | X385 | workspace, orchestrator, and MCP package changelogs omit the new 3.1.2 public API/behavior notes | backlog | same package-changelog completeness class as X310/X212; no new work item |
| 3.1.2-release | exact-SHA confirmation round 2 | docs-schema, orchestrator / triad-fable, triad-gemini | X386 | unreadable raster files and artifact directories could escape as raw filesystem errors instead of a sanitized artifact-security refusal | fixed | 45532377 makes raster reads fail closed; 5638cba5 adds one component-wise path/discovery owner, rejects unsafe roots/parents, and proves ordinary safe nested output still works |
| 3.1.2-release | exact-SHA confirmation round 2 | engine / triad-sol | X387 | non-Git missing-side header normalization rechecks live path existence after diff capture and can race a concurrent create/delete | backlog | explicit non-Git in-place concurrency edge; immutable header provenance is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | exact-SHA confirmation round 2 | engine, orchestrator / triad-sol, triad-fable | X388 | non-Git text diff truncation happens before the secret scan, so a token beyond the 200,000-character projection bound is not inspected | fixed | bcb011b6: the complete non-Git text diff crosses the secret fence before the 200,000-character presentation bound, with beyond-boundary negatives |
| 3.1.2-release | exact-SHA confirmation round 2 | engine / triad-fable | X389 | thread delivery says `patch contains` even when only binary candidate bytes triggered the refusal | backlog | refusal remains safe and typed; diagnostic-owner parity with X361 is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | exact-SHA confirmation round 2 | engine, orchestrator / triad-fable | X390 | legacy worktree wording and one indented architecture paragraph lag the private-clone authority | backlog | narrative-only clone/format cleanup, including Bible concept-change discipline, is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | exact-SHA confirmation round 2 | orchestrator / triad-sol | X391 | an in-place capture or rollback exception can fail generically without the sanitized manual-cleanup WorkProduct receipt | fixed | bcb011b6, 11ab618d, cbffbb64: capture and rollback exceptions materialize sanitized manual-cleanup receipts without persisting the sensitive cause |
| 3.1.2-release | exact-SHA confirmation round 2 | orchestrator / triad-sol, triad-fable | X392 | a secret refusal still persisted individually safe produced media despite the X355 no-output criterion | fixed | 45532377 adds the explicit `persistProducedMedia:false` refusal mode and regressions proving no patch or produced directory remains |
| 3.1.2-release | exact-SHA confirmation round 2 | orchestrator / triad-fable | X393 | invocation-keyed recovery does not treat a later vendor retry with a new non-null tool-use id as recovery | declined | D312-4 and Bible INV-043 intentionally require matching non-null invocation ids and retain tuple fallback only when either id is absent; no code change |
| 3.1.2-release | final full-diff round 3 | triad-sol | X394 | a broad post-confirmation round lacked an explicit owner authorization in the sealed decision registry | fixed | owner decision D312-12 records the explicit 2026-07-26 request for a final full-diff agent review and all approvals; the final packet binds that authority |
| 3.1.2-release | final full-diff round 3 | triad-gemini | X395 | claim: `runCandidateInEnvelope` references an undefined `planText` in `finalizeAttempt` | declined | factually wrong on the exact SHA: the function passes its in-scope `deliverableEvidence`; `planText` appears only in the planner path, and exact orchestrator/test typechecks pass |
| 3.1.2-release | final full-diff round 3 | triad-gemini | X396 | claim: `candidateEvidence.ts` exports `partitionsCandidates` while the orchestrator imports `partitionCandidates` | declined | factually wrong on the exact SHA: the export and import are both `partitionCandidates`, and exact TypeScript gates pass |
| 3.1.2-release | final full-diff round 3 | triad-sol, triad-fable | X397 | the integrated site restored the unconditional all-harness role claim and reverted the accepted X279 Doctor/capability wording | fixed | bca793ce restores conditional Doctor-enabled participation and the unavailable-role reason; the site metadata and format checks pass |
| 3.1.2-release | final full-diff round 3 | sol, fable, Fable-high | X398 | packet `confirmationDiffSha256` accidentally contained a doubled `sha256:` prefix | fixed | packet-only generator error; the final packet records the single canonical prefix and is re-sealed before review |
| 3.1.2-release | final full-diff round 3 | triad-fable | X399 | an artifact-dir raster also linked in answer markdown can be copied twice to one exclusive destination and fail with `EEXIST` | fixed | bcb011b6: all candidate media producers are deduplicated before one exclusive, shared-cap persistence pass |
| 3.1.2-release | final full-diff round 3 | triad-fable | X400 | unprovable media/path risks still use copy that says a secret-like token was positively found | fixed | bcb011b6: the shared refusal copy now says the output could not be proven secret-safe without claiming positive token detection |
| 3.1.2-release | final full-diff round 3 | triad-fable | X401 | non-Git reverse-apply failure copy always implies a concurrent target change because no tree hashes exist in no-index mode | fixed | d4338b8a: no-index reverse-check failure now says exact reversal could not be proven and uses the typed reverse-apply cause without inventing later user edits |
| 3.1.2-release | final full-diff round 3 | triad-fable | X402 | a missing `diff` tool is projected as an uncaptured secret risk rather than a distinct infrastructure-capture cause | backlog | same explicit in-place exception-to-receipt diagnostic class as X391; no new work item |
| 3.1.2-release | final full-diff round 3 | triad-fable | X403 | Git binary-object scanning uses decoded command output while the plain-diff scanner explicitly uses latin1 bytes | backlog | current ASCII token detection remains intact; byte-decoding parity between the two binary fences is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final full-diff round 3 | triad-fable | X404 | the Pages metadata check reads README but the workflow path filter does not include README | backlog | exact candidate is consistent; future trigger parity is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final full-diff round 3 | triad-fable | X405 | the site metadata checker does not include crawler-facing `site/llms.txt` in its legacy-origin sweep | backlog | no stale origin is present in the exact candidate; checker coverage is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final full-diff round 3 | triad-sol | X406 | X359 used a features-like qualified disposition without a FEATURES row | fixed | X359 is normalized to canonical `declined` because D312-10 explicitly accepts the boundary and requires the truthful manual-cleanup receipt |
| 3.1.2-release | final full-diff round 3 | triad-sol | X407 | the X380 disposition map omitted qualified values retained in historical rows | fixed | the ledger header now maps every retained historical value to one of fixed, declined, backlog, or features and requires canonical values for new rows |
| 3.1.2-release | final full-diff round 3 | triad-fable | X408 | the redesigned site retains an unused `canvas#field` CSS selector after moving to the ambient div | backlog | cosmetic dead presentational rule; tracked in `docs/BACKLOG.md` |
| 3.1.2-release | final Fable-high round 3 | engine | X409 | a runner throw releases reserved cash but permanently consumes one local Delegate child-count slot | fixed | bcb011b6: the process-local ledger releases a started slot when the runner throws before creating a child |
| 3.1.2-release | final Fable-high round 3 | engine | X410 | count-cap remediation incorrectly suggests raising the parent budget even though the eight-child cap is fixed | fixed | bcb011b6: count-cap refusal now names the fixed eight-child limit and no longer recommends changing paid budget |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X411 | the site metadata checker does not prove referenced assets/anchors exist and omits crawler surfaces from the legacy-origin sweep | backlog | extends X405 checker coverage; exact candidate assets and URLs are present, and the broader deterministic sweep is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X412 | renamed site section anchors do not preserve previously published external deep links | backlog | no repository-internal stale links remain; compatibility anchors are tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X413 | architecture still describes empty-diff live-tree fallback after non-Git write capture began failing closed | fixed | a904ebea: fixed with X382 |
| 3.1.2-release | owner-authorized final round 4 | triad-sol | X414 | malformed injected Delegate policy JSON can yield NaN headroom or out-of-range depth/count values instead of a typed refusal | backlog | daemon-generated policy is typed, so malformed manual environment input is non-default; validation/clamping is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X415 | JSON quoting of Git alternates is not Git C-style quoting for repository paths containing control characters | backlog | normal macOS paths and the colon-path regression pass; full Git-compatible quoting is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X416 | scratch cleanup failure can both replace the primary capture result and leave private temporary candidate objects behind | fixed | bcb011b6, cbffbb64: the primary capture or revert diagnosis is retained with cleanup failure attached separately, while cleanup-only failure remains terminal |
| 3.1.2-release | owner-authorized final round 4 | sol, fable, Fable-high | X417 | the ledger's canonical-new-row rule contradicted qualified dispositions in the 3.1.2 rows | fixed | every retained 3.1.2 qualified backlog/declined form, including X293, is normalized to the canonical values; historical pre-3.1.2 forms remain explicitly mapped |
| 3.1.2-release | owner-authorized final round 4 | triad-sol | X418 | the backlog preamble's broad ranges also contain fixed and declined rows | fixed | the preamble now states that the ranges include associated context and that the ledger, not the range shorthand, is authoritative for exact disposition |
| 3.1.2-release | owner-authorized final round 4 | triad-sol | X419 | `role="img"` on the network map hides substantive descendant harness descriptions from assistive technology | backlog | the shorter aria-label remains available, but preserving the full text semantics is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable | X420 | durable secret-refusal fields live in local artifact YAML without one schema-owned contract | backlog | current writers/readers are internally consistent and release gates pass; schema ownership for the durable receipt is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final round 4 | triad-fable, Fable-high | X421 | the site redesign retains additional unused nav/vendor selectors beyond the ledgered canvas rule | backlog | the shared dead-presentational-rules item in `docs/BACKLOG.md` explicitly covers the canvas and nav/vendor selectors |
| 3.1.2-release | owner-authorized final Fable-high | engine | X422 | non-Git in-place diff capture does not exclude `.claudexor-artifacts` like Git-mode capture does | backlog | screenshot-only non-Git in-place is explicit and secret scanning remains fail-closed; F4/noChanges parity is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | owner-authorized final Fable-high | engine | X423 | workspace `gitEnv` invocation logic is duplicated between capture and revert owners | backlog | behavior is byte-identical and tests pass; consolidating the environment-scoped Git runner is tracked in `docs/BACKLOG.md` |
| 3.1.2-release | targeted ledger re-check | sol, fable, Fable-high | X424 | X421 pointed at the X408 dead-style class but the backlog bullet named only the canvas selector | fixed | the shared backlog bullet now explicitly covers `canvas#field` and the unused nav/vendor selectors |
| 3.1.2-release | targeted ledger re-check | fable, Fable-high | X425 | X293 remained `declined-with-evidence` after X417 claimed every 3.1.2 qualified form was canonical | fixed | X293 is now canonical `declined` and X417 states the complete 3.1.2 normalization |
| 3.1.2-release | targeted Fable-high | docs-targeted | X426 | historical 3.1.0 ledger rows reuse the finding id X227 for two waves | backlog | pre-existing bookkeeping ambiguity; merge or renumber the duplicate while preserving both wave references, tracked in `docs/BACKLOG.md` |
| 3.1.2-release | targeted ledger re-check round 2 | Fable-high | X427 | the backlog preamble stopped at X423 after the section added X426 | fixed | the preamble now uses an open-ended associated-row statement and keeps each exact disposition authoritative in the ledger, avoiding repeated stale upper bounds |
| 3.1.2-release | exact-SHA causal confirmation | adversarial | X428 | a symlinked raster outside the reversible patch could be reported as reverted after only the symlink itself was removed | fixed | d4338b8a: candidate output risk retains non-reversible path provenance, so external symlink targets force a truthful manual-cleanup receipt |
| 3.1.2-release | exact-SHA causal confirmation | adversarial | X429 | a native attempt that switched to an API route without an API usage receipt could settle exact zero cash | fixed | d4338b8a: route-proof intervals track unresolved API and undisclosed segments independently from native valuation; mixed-route missing cash is cost-unverifiable |
| 3.1.2-release | exact-SHA causal confirmation | adversarial | X430 | isolated capture cleanup failure could still report the candidate as safely discarded | fixed | d4338b8a: cleanup-unproven capture failure materializes a sanitized manual-cleanup receipt while ordinary isolated capture failure remains discarded |
| 3.1.2-release | exact-SHA causal confirmation | scope | X431 | a corrupt legacy child detail dropped its live waiting-on-user state on the diagnostic fallback path | fixed | d4338b8a: the cached fail-soft child projector overlays pending interaction state even when durable child projection is corrupt |
| 3.1.2-release | exact-SHA causal confirmation | scope | X432 | a queued parent retained its job id as SwiftUI identity, so child cards keyed only to the later real run id disappeared | fixed | d4338b8a: the parent keeps stable queued identity plus a resolved server run id used for Delegate lineage matching |
| 3.1.2-release | exact-SHA causal confirmation | triage | X433 | a failed native reviewer with an exact zero partial receipt lost its cash knowledge because zero was treated as absent | fixed | d4338b8a: partial reviewer accumulation preserves nonnegative receipts and component knowledge, including exact subscription cash zero |
| 3.1.2-release | exact-SHA causal confirmation | triage | X434 | outside-patch media and no-index reverse-check failures used recovery copy that implied a concurrent user edit | fixed | d4338b8a: receipts distinguish media outside the exact patch, unproven no-index reversal, and later divergence without inventing a cause |
| 3.1.2-release | synthesis gate | complexity | X435 | integrating route-certainty tracking raised the central attempt telemetry owner above its locked line baseline | fixed | d4338b8a: event-level route and receipt tracking moved to the attempt-usage cost owner; the baseline was not raised and the ratchet passes |
| 3.1.2-release | targeted exact-SHA cleanup confirmation | adversarial | X436 | cleanup-only failure after successful isolated capture threw a plain error, so quarantine could still call the unproven scratch state discarded | fixed | cbffbb64: workspace emits the same typed cleanup-evidence contract for cleanup-only and primary-plus-cleanup failures; quarantine therefore materializes sanitized manual cleanup |
| 3.1.2-release | sealed panel targeted adjudication | orchestrator / triad-sol, triad-fable | X437 | Delegate cancellation or drain failure reconciled terminal facts but left late child cash stale in `decision.yaml`, which Control prefers over event folding | fixed | 8ad848ef: every Delegate-owned cancel/failure path writes the post-drain cash and valuation snapshot before terminal facts; timeout and cancellation negatives settle a late child and assert decision/result parity |
| 3.1.2-release | sealed panel targeted adjudication | orchestrator / triad-sol | X438 | an artifact-persistence throw after streaming lost mixed native/API component spend because the caller received no attempt cost evidence | fixed | 8ad848ef + c46df8b3 + 6eea6484: fallible output persistence transports the full typed cash/valuation settlement to one lease-owner helper; real mixed-route persistence failures pin race, convergence, continuation, and synthesis |
| 3.1.2-release | sealed panel targeted adjudication | panel protocol / triad-fable | X439 | the sealed packet described focused tests run on the product parent rather than the final documentation-only frozen SHA | fixed | c46df8b3: predecessor packets are invalid and excluded from attestation; publication remains blocked until the post-freeze focused battery and full release gate run at the exact final SHA and the external packet is sealed against those receipts |
| 3.1.2-release | sealed panel targeted adjudication | workspace-orchestrator / triad-fable | X440 | successful rollback followed by scratch cleanup failure was reported as if live bytes could not be rolled back | fixed | 8ad848ef: workspace marks rollback-completed cleanup failure separately and quarantine truthfully names reverted live bytes plus unproven Claudexor temporary state |
| 3.1.2-release | sealed panel targeted adjudication | MCP belt / triad-fable | X441 | a runner throw after daemon child creation can free the conservative process-local count slot even though the authoritative child exists | backlog | the daemon family authority still enforces the fixed cap and returns a typed refusal; pre-create versus post-create acknowledgement is tracked in the 3.1.2 external-review advisories |
| 3.1.2-release | sealed panel targeted adjudication | macOS / triad-fable | X442 | a legacy component-less `budget.cash` replay could show approximate zero live while Control normalized the same cash fact to exact zero | fixed | 8ad848ef + c46df8b3: the Swift stream fold mirrors the server-owned legacy exact-zero normalization; legacy and current component-aware replay regressions pin both sides of the compatibility boundary |
| 3.1.2-release | sealed panel targeted adjudication | budget architecture / triad-fable | X443 | attempt and reviewer route-certainty owners duplicate the same interval state machine | backlog | both owners agree and are independently tested on this release; extracting a shared small primitive after release is tracked without risking the frozen batch |
| 3.1.2-release | sealed panel targeted adjudication | review protocol / triad-sol, triad-fable | X444 | several 3.1.2 rows changed to `fixed` without citing their exact fixing commits | fixed | fffd6abd: the affected rows now cite their concrete batch commits; the targeted confirmation packet includes this canonical ledger text |
| 3.1.2-release | sealed panel targeted adjudication | historical docs / triad-sol | X445 | historical X194 and X200 summaries remain truncated in both ledger and backlog despite a prior sentence-boundary repair claim | backlog | pre-3.1.2 historical evidence repair is unrelated to Delegate runtime reachability and is tracked in the external-review advisories |
| 3.1.2-release | sealed panel targeted adjudication | Control compile / triad-sol | X446 | claim: `budgetSnapshot` assigns to a constant `lastCash` and cannot compile | declined | false on the exact reviewed SHA: the declaration is `let lastCash`; exact control-api build and TypeScript test typecheck pass |
| 3.1.2-release | sealed panel targeted adjudication | Control diagnostics / triad-fable | X447 | fail-soft corrupt-child diagnostic text embedded a raw projection error without a final secret redaction pass | fixed | 8ad848ef: the diagnostic owner redacts the complete safe message before schema projection while retaining fail-soft lineage and waiting state |
| 3.1.2-release | sealed panel targeted adjudication | architecture docs / triad-fable | X448 | the clean-survivor secret-refusal race boundary lived only in the decision registry and review ledger | fixed | 8ad848ef: ARCHITECTURE now states the safe-survivor exclusion and the all-refused, in-place, and injected-belt hard boundaries |
| 3.1.2-release | sealed panel targeted adjudication | route certainty / triad-fable | X449 | a finite-cap route-silent adapter now fails cost certainty closed where a subscription reservation previously shielded it | backlog | this is the intentional D312-14 fail-closed direction and default adapters disclose routes; explicit route-disclosure pins for every optional adapter are tracked in the external-review advisories |
| 3.1.2-release | sealed panel targeted adjudication | budget holds / triad-fable | X450 | concern that callers might feed subscription valuation into the now route-neutral `updateHold` owner | declined | exact caller inspection and `attemptUsage` regressions prove only cash plus unknown usage reaches the hold; native exact and estimated valuation never invokes the guard |
| 3.1.2-release | exact-SHA targeted confirmation | orchestrator / triad-sol, triad-fable | X451 | only the primary race catch consumed the typed post-stream settlement, while convergence, continuation, and synthesis replaced the same mixed cash/valuation evidence with unknown | fixed | c46df8b3 + 6eea6484: one attempt-failure accounting owner serves all four live callers; EISDIR persistence regressions prove each lease retains API cash and native valuation |
| 3.1.2-release | exact-SHA targeted confirmation | panel protocol / triad-sol, triad-fable | X452 | X439's disposition used completed-state wording even though the confirmation packet truthfully recorded the full gate as pending | fixed | c46df8b3: the canonical row now states the prospective post-freeze gate, external reseal, and attestation boundary without claiming pending evidence already exists |
| 3.1.2-release | exact-SHA targeted confirmation | macOS parity / triad-fable | X453 | the legacy exact-zero compatibility test did not pin that a current component-aware zero-cash event preserves its explicit estimated bit | fixed | c46df8b3: the Swift replay test now covers both missing and present `valuation_knowledge`, preventing the legacy exception from absorbing current events |
| 3.1.2-release | exact-SHA local adversarial confirmation | convergence projection | X454 | the convergence lease retained transported mixed-route settlement, but its failed `CandidateRun` still projected scalar cost zero | fixed | 7a8beea6: the fallback candidate reuses the same failure-cost owner for its scalar total and estimate flag while ordinary unknown setup failures retain the prior zero/non-estimated projection |
| 3.1.2-release | final exact-SHA confirmation | panel protocol / triad-sol | X455 | the regenerated packet copied a stale `RUNTIME.md` that named the superseded e703 candidate despite the current FREEZE and fingerprints | fixed | packet-only regeneration: the e360 panel output is excluded from attestation; the final packet derives `RUNTIME.md` candidate SHA/tree from the same frozen identity used by FREEZE and rebuilds the complete manifest |
| 3.1.2-release | final exact-SHA confirmation | race projection / local scope, triad-sol, triad-fable | X456 | the race lease and attempt total retained mixed-route cost, but the fallback card omitted `cost_estimated` and kept an internal zero/exact scalar | fixed | 1a1d1f96: the durable attempt and fallback `CandidateRun` reuse `failureCost.totalUsd` plus `failureCost.estimated`; the existing mixed-route EISDIR regression pins the projected estimate bit |
| 3.1.2-release | final exact-SHA confirmation | ledger citation / triad-fable | X457 | concern that X453 cited c46df8b3 although the commit subject names orchestrator rather than macOS | declined | `git show --name-only c46df8b3` includes `AppModelRefreshTests.swift`; commit subjects are summaries, while the cited exact object proves the Swift compatibility test landed there |
| 3.1.2-release | final exact-SHA confirmation | fallback cost certainty / triad-sol | X458 | an ordinary positive `costUsd` fallback carried UNKNOWN settlement knowledge but projected `estimated:false` in its candidate scalar | fixed | 7980f560: positive unknown fallback amounts now project estimated while explicit zero/setup remains non-estimated; positive, zero, and invalid controls pin the boundary |
| 3.1.2-release | final exact-SHA confirmation | convergence artifacts / scope | X459 | a convergence post-stream persistence failure retained settlement in the ledger and candidate but left no minimal `attempt.yaml` artifact | fixed | 7980f560: convergence mirrors race's truthful minimal failed-attempt record with redacted error, scalar cost, estimate bit, and harness phase; the real EISDIR regression pins the file |
| 3.1.2-release | final exact-SHA confirmation | packet hygiene / triad-fable | X460 | `DECLINED_FINDINGS.md` embedded the entire canonical ledger instead of only declined-disposition rows | fixed | the external final-packet builder now filters and count-checks canonical declined rows before secret scan and sealing; the superseded packet and its reports remain excluded from attestation |
| 3.1.2-release | final exact-SHA confirmation | budget event contract / scope | X461 | the Swift legacy discriminator depends on every current `budget.cash` event carrying `valuation_knowledge`, but the producer contract was considered implicit | fixed | 7980f560: the sole current root-ledger producer test explicitly asserts every emitted cash event carries exact, estimated, or unknown valuation knowledge; current and legacy Swift projections remain separately pinned |
| 3.1.2-release | whole-tree release-flow audit | exact artifact promotion / scope, adversarial, triage | X462 | publish rebuilt signed DMG/ZIP and regenerated SBOM instead of promoting the exact candidate app bytes accepted for release | fixed | 7980f560: signing/build are candidate-only; publish binds the candidate run to workflow, success, and exact SHA, verifies all candidate provenance/checksums/signatures/version/build SHA/belt/SBOM, then promotes DMG, ZIP, SBOM, and runtime closure byte-for-byte; scoped mutation controls and public docs pin A-5 |
| 3.1.2-release | release-flow delta confirmation | SBOM source identity / triad-sol, scope | X463 | candidate SBOM generation used ambient `GITHUB_SHA` while publish regeneration used the prepared candidate SHA | fixed | 486be71c: both pipeline processes on both sides now receive `PREPARED_SHA`, and a mutation control removes the candidate binding to prove the checker goes red; differing candidate/dispatch SHAs were already refused at input validation |
| 3.1.2-release | release-flow delta confirmation | packaged Node / triad-sol | X464 | moving signing credential import behind the candidate guard also made `CLAUDEXOR_NODE_BIN` candidate-only while public guidance promised every release workflow pins it | fixed | 486be71c: an unconditional post-setup-node step exports the exact runtime before all gates and builds; the credential step owns only Apple material and a mutation control rejects a mode guard on the Node pin |
| 3.1.2-release | release-flow delta confirmation | SBOM determinism / triad-fable | X465 | concern that reordered `pnpm licenses` groups could make the publish-time byte comparison spuriously refuse an authentic candidate SBOM | declined | the generator already sorts dependency/package output and uses fixed fields, exact app bytes, commit identity, pinned Node/pnpm/lockfile; 486be71c adds a raw byte-identity regression under reversed license-group order and two real local license snapshots were identical |
| 3.1.2-release | release-flow delta confirmation | test compilation / triad-gemini | X466 | claim: `reapMk` refers to undefined `skdtempSync`, so mandatory test typecheck fails | declined | factually false on the reviewed SHA and its history: the import and type annotation both use `mkdtempSync`, repository search finds no claimed symbol, and exact `pnpm typecheck:tests` passes |
| 3.1.2-release | release-flow delta confirmation | candidate identity / scope | X467 | candidate verification checked signatures and bundled resources but deferred the daemon version/build-SHA probe until publish | fixed | 486be71c: the signed candidate ZIP is probed with its bundled Node against exact version and `PREPARED_SHA` before upload; publish retains an independent probe and checker mutations pin both comparisons |
| 3.1.2-release | release-flow delta confirmation | process documentation / scope | X468 | DEVELOPMENT wording placed byte promotion before signed attestation verification even though workflow authority validation happens first | fixed | 486be71c: the text now states authority verification first and only then exact candidate promotion, matching prepare and package revalidation order |
| 3.1.2-release | release-flow targeted confirmation | promoted SBOM checker / triad-fable | X469 | checker pinned the candidate-side prepared SHA but did not require or independently mutate both publish-side SBOM pipeline bindings | fixed | 0e667d29: the promoted-SBOM pattern requires both `GITHUB_SHA=PREPARED_SHA` prefixes and a first-occurrence mutation proves the publish-side check goes red independently |
| 3.1.2-release | release-flow targeted confirmation | promoted daemon checker / triad-fable, scope | X470 | candidate probe mutations now hit the earlier new probe, so the promoted-app version/build comparisons lost independent mutation coverage | fixed | 0e667d29: first-occurrence controls pin candidate comparisons and last-occurrence controls separately pin promoted-app version and build SHA |
| 3.1.2-release | release-flow targeted confirmation | SBOM determinism / scope | X471 | byte determinism regression shuffled only license-group keys, not packages inside a group or versions inside a package | fixed | 0e667d29: the same raw-byte equality now reverses group order, intra-group package order, and multi-version order while the canonical output stays byte-identical |
| 3.1.2-release | release-flow final confirmation | attestation schema checker / triad-sol | X472 | release checker still described the schema-v4 attestation validator as rejecting non-v3 input and only forbade stale schema-v2 wording | fixed | 26c36e7b: the comment names non-v4 truth, one shared fence rejects schema-v2 and schema-v3, and negative controls pin both stale spellings |
| 3.1.2-release | release-flow final confirmation | promoted SBOM checker / triad-fable | X473 | the publish-side license-input mutation did not independently prove the generator invocation retained its prepared-SHA binding | fixed | 26c36e7b: first/last generator mutations now require the exact promoted/candidate scoped finding, independently of the license-input mutations |
| 3.1.2-release | release-flow final confirmation | mutation targeting / triad-fable | X474 | first/last mutation controls assumed each paired workflow needle occurred exactly twice, so a third occurrence could silently retarget both controls | fixed | 26c36e7b: one table owns the four paired needles, requires exact-two cardinality, and uses separate third-occurrence RED cases; semantic mutations must produce their own scoped finding so cardinality cannot mask them |
| 3.1.2-release | release-flow final confirmation | attestation schema negative control / triad-fable | X475 | stale schema self-tests exercised the regex literals but not the same owner wired to the real release workflow text | fixed | cfd4d510: production and both mutated full-workflow controls call one `staleAttestationFindings` owner and require its exact schema-v2/v3 diagnostic, so a detached regex test cannot mask broken wiring |
| 3.1.2-release | release-flow final confirmation | reviewer liveness / owner causal audit | X476 | transport used prompt-size-aware liveness floors, but the final panel decision silently rechecked every responded slot against the default 30-second floor | fixed | a8399fa9: the exact triad and scope prompt lengths now drive both transport and final decision independently; 20.74-second medium-packet records pass at 20 seconds, remain rejected by the 30-second default, and a megabyte-scale scope record still fails at 30 seconds |
| 3.1.2-release | liveness consistency confirmation | production wiring / triad-sol | X477 | the decision regression passed explicit floors directly and did not pin that the live transport derives and supplies both floors from the submitted prompts | fixed | 4fdb35e6: one pure `reviewDecisionLivenessFloors` owner derives the pair from triad/scope lengths; production spreads that exact helper and the regression tests its medium and divergent outputs through the final decision |
| 3.1.2-release | liveness consistency confirmation | scope fail-closed default / triad-fable | X478 | a caller supplying only the triad floor also lowered the scope floor through the parameter default | fixed | 4fdb35e6: omitted scope input again reaches the independent 30-second `reviewerLiveness` default; a triad-only 20-second floor stays NO-GO until the scope's own prompt-derived floor is explicitly supplied |
| 3.1.2-release | exact-candidate preflight | signed packaged entry / CI | X479 | candidate run 30208111763 extracted the signed ZIP under `/var/folders`, while the bundled direct-entry fence compared that alias text to `/private/var/folders` and silently skipped `--probe` | fixed | a6a75ab7: direct entry compares canonical existing-file realpaths; the signed app probes before notarization and the extracted ZIP still probes before upload |
| 3.1.2-release | exact-artifact acceptance design | delegated interaction / scope | X480 | a Delegate child could project `waitingOnUser` but only its compact row rendered, leaving no macOS answer control | fixed | a6a75ab7: the child row renders the ordinary `InteractionCard` against the child run id; design and exact-candidate HID acceptance now pin the behavior |
| 3.1.2-release | exact-artifact acceptance design | Delegate parity / scope | X481 | healthy Delegate parents exposed `requested/effective/used/reason` through Control, CLI, and MCP but macOS rendered only degraded warnings | fixed | a6a75ab7: the run-filtered workspace shows all four exact engine-owned fields plus explicit child lineage without locally re-deriving them |
| 3.1.2-release | bounded fix review | child hydration cache / scope | X482 | a child hydrated before the parent waiting overlay would hit `ensureRunDetail`'s cache and remain forever without its question body | fixed | a6a75ab7: waiting-without-payload uses the existing coalesced `loadRunDetail` owner; stale-cache and no-repeat integration controls pin the transition |
| 3.1.2-release | bounded fix review | compact receipt layout / scope | X483 | the first exact Delegate receipt implementation placed every field and a long reason in one unconstrained FlowLayout item that could overflow the narrow workspace | fixed | a6a75ab7: requested, effective, used, and reason are four compact wrapping facts; exact-value and ordering controls pin the surface |
| 3.1.2-release | bounded fix review | warning semantics / triage | X484 | the first receipt implementation painted the ordinary active `reason=pending` state amber because it treated every `effective:false` value as degradation | fixed | a6a75ab7: one `warningShaped` owner serves both durable warning and facts; pending/startup are neutral while unavailable/partial remain warning-shaped |
| 3.1.2-release | bounded fix review | child detail failure / triage | X485 | a failed child detail request left the row on an endless “Loading delegated question” spinner with no cause or retry | fixed | a6a75ab7: the exact detail error and Retry replace the spinner; a 503-then-success integration proves the second request clears the error and loads the question |
| 3.1.2-release | bounded fix review | direct-entry negative control / adversarial | X486 | the first canonical-path regression used a differently named foreign file, so an unsafe basename-only implementation could still pass | fixed | a6a75ab7: an existing foreign directory now carries the same `claudexord.bundle.cjs` basename, while alias-positive, same-name-foreign, and missing-path cases pin the full-path fence |
| 3.1.2-release | exact-candidate confirmation | interaction routing / triad-sol, adversarial | X487 | a pending interaction rendered under an optimistic queued row could submit through the stable job-id alias instead of the interaction's canonical server run id | fixed | 1227f3a5: the shared `InteractionCard` owns `interaction.runId` for ordinary and delegated rows; accepted-answer cleanup resolves either stable id or `resolvedRunId`, and a real POST regression pins both wire path and local cleanup |
| 3.1.2-release | exact-candidate confirmation | detail-load presentation / triad-fable, scope | X488 | proposal to replace the tested child-detail error prefix with a new typed load-state field | declined | the exact producer and consumer prefix are pinned together by the 503-to-Retry integration; reviewers cited no default-reachable drift or violated invariant, and the attempted broader state split introduced a second failure owner during adjudication |
| 3.1.2-release | exact-candidate confirmation | child projection lag / triad-fable, scope | X489 | concern that `waitingOnUser:true` with a successful empty interaction detail could retain the loading state until another refresh | declined | the daemon's journal-first projector derives the overlay and pending payload from the same owner, so the panel and scope reviewer both capped this as non-default-reachable; the existing concrete transport-failure path remains retryable |
| 3.1.2-release | exact-candidate confirmation | app-only signing / triad-sol, triad-fable | X490 | the signed probe references bundled-engine paths when `CLAUDEXOR_NO_ENGINE_BUNDLE=1` | declined | release CI always bundles the engine, while an ad-hoc signed app-only build already fails earlier on the mandatory packaged helper; the panel marked the path unreachable and no release acceptance criterion permits an engine-less signed candidate |
| 3.1.2-release | exact-candidate confirmation | public release notes / triad-fable | X491 | the root 3.1.2 release note did not explicitly name inline child answers or canonical temporary-path candidate probing | fixed | 1227f3a5: the root changelog now states both shipped acceptance facts; release.yml continues to derive public GitHub notes from that root entry |
| 3.1.2-release | exact-candidate packet audit | declined-finding authority / local scope | X492 | the confirmation packet generator selected only literal `declined` rows and omitted canonical `declined-with-evidence` and `SUPERSEDED-declined` dispositions | fixed | packet-only operator fix: all three canonical declined dispositions are parsed by the disposition column and the complete set is count-checked before manifest sealing; the incomplete packet and its panel output are excluded from attestation |
| 3.1.2-release | exact-candidate packet audit | path enumeration / local scope | X493 | the confirmation packet generator enumerated changed files with newline-delimited `git diff --name-only` instead of the NUL-safe review owner | fixed | packet-only operator fix: the generator now consumes `git diff -z --name-status` through `parseNameStatusZ`, preserving rename/copy and whitespace/unicode path semantics before manifest sealing |
| 3.1.2-release | final confirmation adjudication | ledger hygiene / triad-fable | X494 | new X488-X490 rows used historical `declined-with-evidence` instead of the ledger's canonical new-row disposition | fixed | 6b00025e: all three rows now use canonical `declined`; their evidence remains in the reference column and the packet filter still accepts qualified historical rows |
| 3.1.2-release | final confirmation adjudication | receipt docs / scope-fable | X495 | `RunFacts.headerDetails` still described a Details disclosure while the design contract and current layout render the facts in the run-filtered workspace facts row | fixed | 6b00025e: the owner comment now names the actual always-rendered facts row without changing runtime behavior |
| 3.1.2-release | final confirmation adjudication | Swift test hygiene / scope-fable | X496 | source-substring assertions for the shared `InteractionCard` owner can be sensitive to harmless file-layout or spelling refactors | declined | established repository test style pins cross-view wiring while `interactionAnswerUsesCanonicalPendingRunIdAcrossQueuedAlias` separately proves the real POST path and local state transition; no runtime invariant or release acceptance criterion is violated |
| 3.1.2-release | final text confirmation | facts-owner wording / scope-fable | X497 | sibling RunFacts comments still named the retired Run Detail header and a separate Details disclosure after X495 aligned only `headerDetails` | fixed | abe3c36c: the file owner and primary/supporting comments now describe the run-filtered workspace facts row consistently; runtime composition is unchanged |
| 3.1.2-release | final text confirmation | packet ledger wording / scope-fable | X498 | X492 described the declined-row count as 64 even though later canonical declined findings legitimately increased the sealed set | fixed | abe3c36c: X492 now states the stable contract, complete canonical disposition parsing plus a pre-seal count check, without freezing an incidental historical cardinality |
| 3.1.2-release | exact-artifact computer-use acceptance | Cursor Plan transport / live Anka | X499 | native Cursor Plan terminates through `createPlan`, whose fixed tool schema cannot carry the mandatory model-authored WorkReport, so an otherwise usable plan becomes a contract failure and falls back | fixed | ec480117: owner-approved 2026-07-26 D-16/D-31 reconciliation; the Cursor Plan route uses Cursor's explicit read-only Ask transport, where the existing validated fence instruction remains model-authored; exact-argv, createPlan-precedence, and adapter-to-orchestrator composition regressions pin the boundary, and exact-candidate Cursor-only Plan acceptance is required before publication |
| 3.1.2-release | final Cursor confirmation | Cursor Ask fixture parity / gpt-5.6-sol | X500 | the new native Ask plan stream was covered only by inline synthetic frames, despite the adapter fixture rule requiring a provenance-declared stream fixture and conformance parity when native parsing changes | fixed | 02e7de8c + 01b92292: a real Cursor Agent 2026.07.23-e383d2b read-only Ask stream recorded in the credentialed Anka VM is sanitized and provenance-stamped, replayed through the production plan-intent parser with exact final/thinking/delta/tool-error parity, and paired with a synthetic stray-createPlan precedence fixture; the exact VM strict freshness gate passes at zero drift |
